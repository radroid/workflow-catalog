# @workflow-catalog/job-assistant

Placeholder. This becomes `workflow.json`, `skills/`, `schemas/`,
`templates/`, `fixtures/`, and one eve adapter (`adapters/eve/`) for the
job-application assistant workflow package — see `docs/spec/mvp-spec.md` §2
and `ARCHITECTURE.md`. Filled in by packet **P01** (workflow package and
contracts).

# Follow-ups from the manual session's merge gate

One line each: packet, finding, file. The orchestrator appends this list at each merge; implementers and reviewers never edit it. Follow-ups never become new packets or grow another packet's scope. The list is triaged once, in `OVERNIGHT-REPORT.md`.

- P05 · F1: through Z6, a claim's joined bracket that mentions someone else's role states that title. With "Platform Engineer (Senior, reporting to the CTO) at Fernwood Labs", "Served as CTO at Fernwood Labs from 2019 to 2021 [C11]." passes (refused at 451398e; the unbracketed claim already let it pass). · `runner/validate/facts.ts`
- P05 · F2: a cost of Z6: "Platform Engineer (Senior, reporting to the CTO) at Fernwood Labs, 2019–2021 [C12]." is refused ("cto") against a claim without that bracket. · `runner/validate/facts.ts`
- P05 · F3: a cost of Z7: an adjective before "scores" (high, low, top, perfect, …) no longer makes it a noun, so "Kept perfect scores of 100 …" is refused as the quantity "scores of". · `runner/validate/facts.ts`
- P05 · F4: a cost of Z7: a determiner before a noun use counts, so "Kept the scores of each Lighthouse accessibility audit … at 100 [C13]." is refused. · `runner/validate/facts.ts` (`scoresIsNoun`)
- P05 · F5: a cost of Z8: "I am the engineer on call for the payments infrastructure team at Northwind Labs [C1]." is refused ("engineer"), like the recorded Z3 cost for "I was". · `runner/validate/facts.ts`
- P05 · F6: revision 5 has no report of its own; the gate note in the packet's Report and `/tmp/wc-manual/P05-gate-review.md` stand in for it. · `docs/spec/implementation/P05-preparation-and-validator.md`
- P06.1 · F1: K3's counted form (it names the first failure when not every name fits) has no test. `runner/ui/assets/jobs.js:953`, `runner/test/jobs-page-followups.test.ts`
- P06.1 · F2: K4's third case (focus on the posting text itself) has no test. It works live. `runner/test/jobs-page-followups.test.ts`
- P06.1 · F3: The K4 test's title says "…and a chmod'd directory does the same", but its body never chmods. The report's K4 entry says the body covers a chmod'd directory. `runner/test/jobs-page-followups.test.ts:290`, `docs/spec/implementation/P06.1-jobs-and-status-followups.md`
- P06.1 · F4: The report says all 32 shots are at scale 2 (780×1688 or 2560×1800). 12 are at scale 1, though every width is true. It also calls the kept Jobs shots "already correct", but two sets predate K3 and K7. `docs/spec/implementation/P06.1-jobs-and-status-followups.md`
- P06.1 · F5: At 390, the two-line clamp cuts the K5 refusal before the file name ("…(jobs/fc0717d2-a1b1-4ad9-a8c…"). The full text is in the live region and `title`. `runner/ui/assets/jobs.js:1074`
- P06.1 · F6: `clearUnreachable` leaves a stale "Can't reach the runner" line if a watched job disappears during the outage. `started.size > 0` at clear time, and the settle step then drops that entry silently. Adversarial. `runner/ui/assets/jobs.js:982`
- P06.1 · F7: The shared `code { overflow-wrap: anywhere }` breaks short ids mid-id in tables (the Status device id "b92ef / 9da" at 390). This is a net improvement over the old 14 px card overflow. `runner/ui/assets/runner.css:63`
- P06.1 · F8: The Status devices table at 390 still puts Revoke about 11 px past the card's padding. Pre-existing, and reduced by K10. `runner/ui/status.html`, `runner/ui/assets/runner.css`
- P06.1 · F9: `#page-error` on Status is never cleared once an action succeeds, so a runner-down message stays after recovery. Pre-existing. `runner/ui/assets/status.js:29`
- P06.1 · F10: There are no Status screenshots of the busy state (K9's target page), the K8 "Can't reach the runner" result, the Fix-line `<code>`, or K11's pairing errors. All were checked live. `docs/screenshots/`
- P06.1 · F11: The report notes an unhandled rejection: the initial `Promise.all(…).catch(showError)` resolves after the test window closes. It didn't reproduce here, but it can flake CI. `runner/test/status-page-followups.test.ts`, `runner/ui/assets/status.js:171`
- P06.1 · F12: The 1280 `P03.2-status-model-check-failed-*` and `P06.1-status-model-check-success-*` shots predate K8's `<code>` eve URL. They are stale, but still meet 4.3's goal (no setup line). `docs/screenshots/`
- P05.1 · F1: The corpus omits round 1. Deliverable 1 names `/tmp/wc-rev-p05-r{1,…,5}-probes/`, and 35 of the 39 probes in `r1-probes/validator-probe.ts` are in no row (their verdicts are unchanged between head and base). The Report narrows to "rounds 2-5" without saying why. File: `runner/test/fixtures/validator-probes.json`.
- P05.1 · F2: The corpus doesn't pin the three realistic-document probes: r2 PASS, r3 REFUSE at "now use", r3-realistic-reworded PASS. So packet finding 5's third example, "…tooling that three engineering teams now use [C3]", is in neither the corpus nor the known-limitations list. File: `runner/test/fixtures/validator-probes.json`, and the packet Report §3.
- P05.1 · F3: The known-limitations list leaves out finding 7's "(promoted to Staff in 2020)" (r5#35, refused only by `date` for the year, never by the title rule). It also never mentions Deliverable 3 (messages). File: the packet Report §3.
- P05.1 · F4: The finding 5 line swaps its row labels: "remain consistent" is r4#24 and "what is today" is r4#23. File: the packet Report §3.
- P05.1 · F5: The Report has no Acceptance → test map. For bullet 3 (the realistic resume and letter) it names no test; the covering test is `validator.test.ts` › "revision 2: an honest resume and cover letter still pass every rule". File: the packet Report.
- P05.1 · F6: `pins N rows` asserts only `rows.length > 0`, so deleting fixture rows fails nothing. The Report's "per-source counts" overstates what it checks. Pin the exact counts, for example r5 = 101. File: `runner/test/validator-probe-corpus.test.ts`.
- P05.1 · F7: The corpus replays every row as a resume "Experience" bullet and compares only person-facing rule names. The gate's 14 `letter()` probes and the r2b/r3 letter probes no longer go through the cover-letter path, and `forModel` (r2 section G, r4b "model:") and refusal messages aren't pinned. File: `runner/test/validator-probe-corpus.test.ts`.
- P05.1 · F8: Fix 1 also changes how claims are read, and no test pins it: A claim "Engineering manager at Harbor from the last month of 2021." is now open, so "…since 2021 [C9]", "…from 2021 [C9]" and "…, 2021–present [C9]" go REFUSE → PASS. That matches how "from the end of 2021" was already read. A claim "…from 2021 through the last month of 2023." is now closed, so "…since 2021", "…from 2021" and "…in 2021" go PASS → REFUSE. That closes a real hole in the base. "from 2021 through the month of 2023" now passes too. File: `runner/validate/facts.ts`.
- P05.1 · F9: After fix 1, "from 2021 through last month 2023 [C10]" (no "the … of") is still refused (`date`). This is a too-strict leftover of finding 3's shape. File: `runner/validate/facts.ts`.
- P05.1 · F10: Fix 2 adds too-strict costs whose message is a bare `uncited`. "Served under GEN. Quill in the Fernwood reserve signals unit, 2015–2018 [C16]." (base PASS) is now refused with no hint, because the hint covers only 1–2-letter words. "LT." and "MT." do get the hint. This is against Deliverable 3. File: `runner/validate/text.ts` / `runner/validate/validator.ts`.
- P05.1 · F11: The one loosening outside the targeted rows, from fix 2's split: "Platform Engineer at Fernwood Labs, 2019–2021 [C9], near FT. Worth, then Staff engineer at Harbor [C11]." was REFUSE (`date`, C11's end year) in the base and is PASS at the head. Each half is honest against its own citation, so no dishonest text gets through, and the input needs a mid-sentence citation. File: `runner/validate/text.ts`.
- P05.1 · F12: The corpus statements r3#58 and r3-realistic-reworded#58 contain the bare real domain "x.com". It isn't person data and `check:fixtures` passes (there's no scheme), but the fixtures policy allows only `*.example` hosts. File: `runner/test/fixtures/validator-probes.json`.
- P06 · F1: The session manifest and command items carry `jobRevision` (the job snapshot's) but no `Application.revision`. A file-bridge extension can't know the `expectedRevision` its `application_status_changed` must name, so an exported Applied is refused as stale unless it guesses right. It needs a contract change, for example `revision` on `sessionManifestItemSchema` and `openApplicationGroupItemSchema`. packages/contracts/src/session.ts, bridge-envelopes.ts
- P06 · F2: A bridge `stale_revision` refusal (409) carries only code and message, and the bridge has no read route. So the extension's only way to learn the current revision after a refusal is to post another `browser_command_result` with a new eventId. The refusal could carry the current revision and stage. runner/store/sessions.ts, runner/server/routes/sessions.ts
- P06 · F3: The `POST /events` `result` bodies (`items[].{taskId,stage,revision}` for a tab report, `{taskId,outcome,stage,revision}` for a status) have no schema in the contracts. The extension parses an untyped body, and `bridge-client.postEvent` currently drops `result`. packages/contracts/src/bridge-http.ts
- P06 · F4: The adapter README still shows P02's stub body of `open_application_group` ("P02 ships it as a stub that returns 'not implemented'"). It also routes `application_status_changed` to `routes/applications.ts`, but P06 put it in `routes/sessions.ts`. packages/job-assistant/adapters/eve/README.md
- P06 · F5: "Write to outbox" is offered on a session whose command still waits for the paired browser, with no warning. Live, it wrote such a session to `outbox/`, so the group can open twice: once from the bridge command, once from the file import. `create()` guards this case (`already_waiting`), but `writeOutbox()` doesn't. runner/store/sessions.ts, runner/ui/assets/sessions.js
- P06 · F6: An `inbox/` status change is decided with no device check (`deviceId` undefined) and without the bridge journal. So an event the bridge refused (for example 403 `task_not_for_device`) is decided afresh when imported. This is by design for the file bridge, where the person clicks Import, but it isn't stated anywhere. runner/store/sessions.ts
- P06 · F7: An application moved to Applied (by an import or the board) while its session's command is still queued stays in that command, so the browser still opens its tab. Live, the Sessions page showed "Applied · no report yet · you chose Applied" beside "Waiting for your browser to pick it up." runner/store/sessions.ts
- P06 · F8: A `waiting` application whose `preparation.json` is missing or unreadable falls through `stateOf` to idle. Its card then shows neither "Needs your answer" nor a failure; before P06 it showed the failed line. runner/server/routes/applications.ts
- P06 · F9: The run log still records a parked preparation as outcome `failure`, "Waiting for your answer to 2 questions.", while the board says "Needs your answer". The Runs page and the board disagree about the same run. runner/server/routes/applications.ts
- P06 · F10: "It only ever creates new files" (the tool's comment and `store/sessions.ts`'s header) is inexact. With no paired browser, `create()` replaces `outbox/application-session.json` and rewrites the new `state.json`. Also, `already_waiting` in eve's process isn't serialised with the bridge's `SESSION_CHAINS`. runner/agent/tools/open_application_group.ts, runner/store/sessions.ts
- P06 · F11: A name cut at a word can drop the company, which is what tells two cards apart. Live: "Not moved: “Staff Engineer…” changed since…" for "Staff Engineer · Northwind Labs". runner/ui/assets/board.js
- P06 · F12: `board.css` and `sessions.css` still carry their own `.button[aria-disabled="true"]` rule, which P06.1 put in `runner.css`. The copies are identical; remove them. runner/ui/assets/board.css, sessions.css
- P06 · F13: The property test's `import_events` only re-imports events the bridge already delivered. No seed imports a status change that only the file bridge carries, so a first import racing a board move isn't exercised. runner/test/sessions-property.test.ts
- P06 · F14: `#finishMove`'s crash recovery treats "Applied at expectedRevision+1" as done. If anything else wrote the application between the stop and the retry, an Applied that did land is recorded as `refused`. runner/store/sessions.ts
- P06 · F15: The inbox list keys its Import buttons and events by list position (`import-0`, `event-0-0`). A file added or removed between refreshes can shift which file a focused button names. runner/ui/assets/sessions.js
- P06 · F16: `runner/validate/`, which the tool reaches through `store/sessions.ts` → `store/applications.ts` → `validate/draft-schema.ts`, isn't in `lib/build.ts`'s build-stamp trees, so an edit there may not trigger a rebuild. This predates P06: P05's `agent/lib/prepare-*.ts` already import it. runner/lib/build.ts
- P08-B · F1: `runs.js`'s P-a/P-b wording has no automated test. I verified it live at 390 and 1280, in light and dark: a lone folder reads "1 folder could not be read:", "No runs yet." is hidden, and a folder is listed first in a mix. `runner/ui/assets/runs.js`
- P08-B · F2: Carried P-a's Settings half isn't done. With `runs/` unreadable, the budget note still says "Resume saves them and restarts runs", and Resume answers "Error: The runner hit an unexpected error.". `runner/ui/assets/settings-budget.js`, `runner/server/routes/runs.ts`
- P08-B · F3: Carried P-c isn't done, though the Report says Part A shipped it. The note still ends "Resume can't clear this pause." (the exact sentence P-c flagged), with no "check its permissions". With a stored pause on top, usage reads "unknown" with no reason. `runner/ui/assets/settings-budget.js`, `docs/spec/implementation/P08-schedules-runs-budget.md`
- P08-B · F4: The carried P05-f item asked for a real-`Client` test. The four new authorization tests use a scripted fake `Client` (`{ sessions: { create } }`), though the Report says "real `Client`". The existing real-Client pattern is `createEveGateway` with a stubbed `fetch` (`test/onboarding-extract-timeout.test.ts`). `runner/test/run-harness.test.ts`
- P08-B · F5: A daily fire whose one model call hit a provider limit, or whose every job was refused, is recorded `succeeded: true`, so Settings shows it under "Last successful run". Probe B: "started 2 … refused 1", 1 call, budget paused. The PR's own normal-state screenshot shows "refused 1" under "Last successful run". `runner/scheduler/dispatch.ts:166`
- P08-B · F6: The Runs page never shows the scheduler's cap reason. Each preparation is its own `withRun`, with `remainingItemIds: []`, so `itemCapNote` shows nothing. The reason reaches only the console log and Settings' "Last attempt" line. The PR's `runs-catchup-capped-*` shots show a hand-seeded record the scheduler never writes. `runner/scheduler/dispatch.ts`, `docs/screenshots/P08-B-runs-catchup-capped-*.png`
- P08-B · F7: "1 job stay Saved" should be "1 job stays Saved" (Settings summary and log). The summary also reads as raw counters: "attempted 3 of 4: started 3, already prepared 0, already running 0, re-exported 0, refused 0.". `runner/scheduler/dispatch.ts:102,162`
- P08-B · F8: While the budget is paused, each schedule card still shows "Next run …", with no hint that it won't run. `runner/ui/assets/settings-schedules.js`
- P08-B · F9: "Paused: paused from Settings" repeats itself, and the announcement "Schedule paused." doesn't say which schedule. `runner/ui/assets/settings-schedules.js`, `runner/server/routes/runs.ts:120`
- P08-B · F10: A failed pause or resume (for example, `scheduler/` read-only) announces only "Error: The runner hit an unexpected error.", with no next step. `runner/server/routes/runs.ts`
- P08-B · F11: The description says "09:00 UTC", but "Next run" shows local time ("5:00:00 AM") with no zone label. `runner/ui/assets/settings-schedules.js`
- P08-B · F12: The Settings intro still says "Today that is the run budget; pairing is on the Status page." now that Schedules is there. It is outside P08-B's section grant. `runner/ui/settings.html`
- P08-B · F13: A schedule the person paused uses the amber "decision" badge and notice. That matches the budget pause, but the person's own pause isn't a pending decision. `runner/ui/assets/settings-schedules.js:75,97`
- P08-B · F14: The first start of a fresh install fires both schedules at once, as "catch-ups" for fires that never happened. That includes a weekly-review model turn over zero applications, and the review's reply is discarded (only the RunRecord is kept). `runner/scheduler/dispatch.ts`
- P08-B · F15: The claim is made before the work, so a crash mid-fire loses that slot (at-most-once). The next fire does pick up the still-Saved jobs. The design note doesn't say this. `runner/scheduler/dispatch.ts:152`
- P08-B · F16: `readStateFile` reads a corrupt or unreadable `scheduler/state.json` as "nothing paused", and the next write overwrites it. A person's pause is then silently dropped (fail-open), unlike the budget's fail-closed pause. `runner/scheduler/store.ts:56`
- P08-B · F17: `nextFireAt` steps in whole 24 h days (and 7-day weeks) of UTC, so a zone with DST drifts an hour across a change. The "correct across DST" comments are unproven: only UTC ships, and Asia/Kolkata has no DST. `runner/scheduler/time.ts:111-114`
- P08-B · F18: Nothing pins `weekly-review.md`'s content: emptying the file fails no test. CLAUDE.md asks that every prompt-file change ship with the fixture that proves it. `runner/test/scheduler-dispatch.test.ts`
- P08-B · F19: The route test "survives a restart" never restarts (it keeps the same workspace handle). The store test is the real proof. `runner/test/routes-runs-schedules.test.ts:59`
- P08-B · F20: The Part B Report is missing parts the brief requires: the mutation proofs, the list of edited assertions (none were edited) and the CI run id. It also says 16 screenshots; there are 12. `docs/spec/implementation/P08-schedules-runs-budget.md`
- P08-B · F21: `waitForPreparationQueue` is documented "Test-only", but the scheduler now uses it in production. The doc line belongs to P06's file. `runner/server/routes/applications.ts:495`
- P08-B · F22: Flaky unhandled rejection: `status.js` `showError` runs after the page's teardown. It failed one parallel `pnpm test` and passed sequentially. `runner/test/status-page-followups.test.ts`, `runner/ui/assets/status.js:31`
- P08-B · F23: Nit 4 is proven at store level only. No route test covers `GET /api/runs/:runId` with `runs/` unreadable. The Flow 4 rewrite in ARCHITECTURE.md goes beyond the "matching index lines" grant. mvp-spec §8 still describes a `last-successful-run` marker deciding catch-up, while the code decides by slot claims. `runner/test/routes-runs.test.ts`, `ARCHITECTURE.md`, `docs/spec/mvp-spec.md:141`
- P10-A · F1: Step 2 lists only the workspace and provider prompts. Setup also asks for the model slug ("ChatGPT model slug?", default `gpt-5.6-luna`, which is mode A condition 1), or for an API key with an API provider. docs/pilot/success-test.md
- P10-A · F2: Chrome UI wording. A newly loaded unpacked extension goes into the Extensions (puzzle) menu until pinned, not straight onto the toolbar. The action's right-click item is "Options"; "Extension options" is the label on the Details page. docs/pilot/success-test.md
- P10-A · F3: Step 12 (P07-C) never says how to open the side panel. docs/pilot/success-test.md
- P10-A · F4: Step 13 mentions only daily-prepare firing at step 4's first start. Weekly-review also fires on the first start after Monday 09:00 UTC. That is a real provider turn, and it counts against today's run budget (`scheduler/dispatch.ts`), which also affects step 14. docs/pilot/success-test.md
- P10-A · F5: Step 14's forceable case assumes zero runs so far. After step 10's three preparations, setting the limit to 1 shows "Daily limit reached" at once and the *first* Prepare refuses. Two Saved jobs may not exist by then either. docs/pilot/success-test.md
- P10-A · F6: Step 14: after a provider-limit pause, Resume always says "Runs resumed.". "Runs resumed with the default limits…" appears only when `runs/budget.json` was unreadable (`store/budget.ts:253`, `settings-budget.js:135`). docs/pilot/success-test.md
- P10-A · F7: Step 15 doesn't say to stop the runner (Ctrl-C) first, even though step 4 says to leave it running. From reading the code (not reproduced): workspace writes `mkdir -p` their parent, and the 5-minute scheduler tick can claim the deleted slots again, so a live bridge can recreate files under the removed workspace. docs/pilot/success-test.md
- P10-A · F8: The intro says ticket 04's ~30 minutes covers "install through approving" (steps 1–8). Ticket 04 says "installs from the catalog in about 30 minutes". docs/pilot/success-test.md
- P10-A · F9: "After the checklist" omits spec §10's "Plus" items: the nine browser gates green, hostile fixtures green, the catalog live on Hobby at $0, and learning docs reachable from the catalog. docs/pilot/success-test.md
- P10-A · F10: The catalog-deploy fallback skips the catalog entirely. The brief named "the local catalog" (`pnpm --filter catalog dev`) as the fallback, so the invite, install page, checklist and `/learn` are never exercised. docs/pilot/success-test.md
- P10-A · F11: "Provider API keys live only in the OS keychain." Setup also accepts the key from the environment ("using OPENAI_API_KEY from the environment…; nothing stored", `lib/setup.ts:178-181`), which spec §7 rule 4 allows. docs/pilot/privacy-checklist.md
- P10-A · F12: The career-profile line cites §5's "Claim" and "Session manifest" paragraphs. The proof is §5's `career-profile.json … approval{version, at}` listing line. docs/pilot/privacy-checklist.md
- P10-A · F13: "Nothing personal, ever" doesn't say what the catalog does keep: `sessions.display_name` (1–40 chars, chosen at invite), install-checklist ticks, and hashed invite tokens. List them so the owner can check. docs/pilot/privacy-checklist.md
- P10-A · F14: The install-page line cites the spec as its proof. The text is at `apps/catalog/app/(gated)/install/page.tsx:132-133`. docs/pilot/privacy-checklist.md
- P10-A · F15: Forget's workspace removal has two more conditions: the workspace must be recorded in `.env.local` (`workspaceSource === "file"`), and forget never removes the disk root, the home folder, or a folder containing the runner (`lib/forget.ts:79-99`). docs/pilot/privacy-checklist.md
- P10-A · F16: "Deleting the repo leaves nothing behind" is untrue after `--keep-workspace`, or when eve's shared sign-in is kept (the default). docs/pilot/privacy-checklist.md
- P10-A · F17: "The side panel's command poll and its 15-minute alarm re-arm on wake" describes P07-C behaviour with no "per P07-C" marker. docs/pilot/known-limitations.md
- P10-A · F18: "The extension builds before the agent" means the eve extension (the adapter), but it reads as the Chrome extension. `runner/README.md` calls it "the adapter". docs/pilot/known-limitations.md
- P10-A · F19: Not required but person-facing: pairing lives in `storage.session`, so quitting Chrome un-pairs. The options page says so, and each restart needs `npm run pair`. Also missing: the extension README's own "pairing at the exact moment an old token is refused" limitation. docs/pilot/known-limitations.md
- P10-A · F20: Lesson 0003 section 3's pin list (`runner/package.json`, `eval-agent/`, the lockfile) omits the adapter package, which `runner/README.md` says is also pinned exactly. docs/learn/lessons/0003-upgrading-the-pinned-eve-version.html
- P10-A · F21: Lesson 0002's nav has no "Next lesson →" link to 0003 (0002 is outside this part's Owns). docs/learn/lessons/0002-where-the-prompts-live-in-eve.html
- P10-A · F22: The Report's acceptance map says every URL and label was checked. The UI check tested files (`test -f`), not the served URLs, so B1 slipped through. docs/spec/implementation/P10-versioning-and-pilot-readiness.md
- P10-A · F23: Flaky runner test under parallel load: an unhandled rejection at `runner/ui/assets/status.js:31` (`$("page-error")` is null after teardown) fails `pnpm test` with every assertion green. It predates this PR. runner/test/status-page-followups.test.ts
- P07-C · F1: After a browser restart the pairing is gone, and pairing again makes a new device. The runner then refuses Reopen's later report (`command_not_for_device`), and every Applied or Defer on that session is refused locally. Yet the panel still shows Applied and Defer, and MANUAL-GATES gate 3 doesn't say what to expect. Say it up front, and add the step to the checklist. This comes from the pairing's lifetime plus P06's device binding. `extension/src/sidepanel/render.ts`, `extension/MANUAL-GATES.md`
- P07-C · F2: A session card's own outcome line (for example "Opened “Apply today”: 2 tabs in one group.") takes precedence over its report-problem and pending lines. So a refused report or waiting updates stay hidden behind a stale success line. `extension/src/sidepanel/render.ts` (`sessionStateLine`)
- P07-C · F3: Focus drops to `<body>` after "Open a new group anyway" when every task already has a choice, and after "Keep what opened": no `nextFocus` is set, and the pressed button is gone. `extension/src/sidepanel/main.ts` (`runOpen`, `settle`)
- P07-C · F4: The options page's import outcome ("Added to the side panel …", "This session is already in the side panel …") is rendered outside any live region, so it is never announced. `extension/src/options/main.ts` (`importResult`)
- P07-C · F5: `refreshRevisions` reports every task without a recorded tab ID as `closed`. After a restart, a restored tab that is still open therefore gets a spurious `closed` flag on the runner when the person presses Refresh. `extension/src/session/report.ts:337`
- P07-C · F6: A command that attaches to a file-imported session keeps the file's items and URLs, not the command's. It is never compared with them. `extension/src/session/receive.ts:59-67`
- P07-C · F7: Two kinds of command are remembered but never reported, so the runner re-delivers them every lease until they expire: one that the policy refuses for a session first imported from a file, and a second commandId for a session already taken in by command. `extension/src/session/receive.ts:60-63`
- P07-C · F8: Carried from P06 F1: a choice on a file-imported session is kept locally only, and `completion-events.json` carries only bridge sessions' events. It needs an application revision in `SessionManifest`. `extension/src/session/report.ts` (`chooseStatus`)
- P07-C · F9: Two test titles overclaim.
- P07-C · F10: The edited startup assertion could stay exact: `[...fake.alarms.keys()]` should equal `["session-poll"]`. Today it only checks that the retry alarm is absent. `extension/src/worker/index.test.ts:196`
- P07-C · F11: Gate 9's "revoked provider credential" and "hostile localhost request" have no test or checklist line mapped in P07's Report. Both are runner-side, so point to the P02/P03 tests or add a line to the checklist. `extension/MANUAL-GATES.md`
- P07-C · F12: The options import preview still names items by raw task UUID, session id and "rev N". This predates part C (part B), but part C builds on it. `extension/src/options/main.ts` (`renderSessionSummary`)
- P07-C · F13: The iframe heuristic falls back for any page with no JobPosting JSON-LD whose visible frame covers 40% or more of the viewport, such as a large map or video embed. That errs on the safe side (never a wrong save) but will refuse some real postings. `extension/src/capture/extractor.ts` (`postingLooksFramed`)
- P07-C · F14: After Applied, remaining step 4 shows "done" while steps 2 and 3 never do. Every task on one board is titled by its host ("jobs.example") and differs only by path. `extension/src/sidepanel/render.ts` (`stepsList`, `describeAddress`)
- P07-C · F15: `wcSession:` and `wcCommand:` records in `storage.local` are never pruned, so they grow with every session ever received. `extension/src/session/store.ts`
- P03.1 · F1: The paste-a-token route has no workspace or log grep. `bridge.logs` captures only warn and error, so a token logged at info level on any path passes every test (a `ctx.log.info(token)` probe left all 33 tests green). runner/test/onboarding-sources.test.ts
- P03.1 · F2: No test runs a document, archive or page source through `/extract` with a scripted non-`extract_claims` action to show only verified `extract_claims` output persists. There is also no PDF or DOCX hostile-storage test. The bullet rests on the Report's recorded check plus the P03 eval. runner/test/onboarding-sources.test.ts
- P03.1 · F3: Carried item 2 is only partly done. The page-load lines keep the capital after the colon ("couldn't load: The profile is busy.") and still show the browser's own "Failed to fetch." (Safari and Firefox word it differently). The test pins that wording. runner/ui/assets/onboarding.js, runner/ui/assets/profile.js
- P03.1 · F4: An existing test was edited outside Owns. The edit was forced by the `fixtures/onboarding/` grant and disclosed, and the file-level assertion is unchanged; the orchestrator should ratify it. packages/job-assistant/test/fixtures.test.ts
- P03.1 · F5: The GitHub token field is `type="text"` with no `autocomplete="off"`. The pasted token shows in clear, and stays visible after a failed save. runner/ui/assets/onboarding.js
- P03.1 · F6: After Save token, focus falls to the page title, because the Save button is removed while the status reloads. At 390 this scrolls the view to the top. runner/ui/assets/onboarding.js
- P03.1 · F7: The binary file input's `aria-disabled` busy state has no visible style; only `.button` has one. runner/ui/assets/onboarding.css
- P03.1 · F8: There is no client-side size check. The page reads and base64-encodes a file of any size before the server's 10 MB refusal. runner/ui/assets/onboarding.js
- P03.1 · F9: The Sources intro still says "paste its text or upload a .txt or .md file", with no mention of the new modes. The README says the modes are in `onboarding.html`, which is unchanged. runner/ui/onboarding.html
- P03.1 · F10: Commands are not in `<code>`: "gh auth login" and "gh CLI" in the GitHub note. The server's `no_token` message shows literal backticks. runner/ui/assets/onboarding.js, runner/lib/github-source.ts
- P03.1 · F11: A status code is visible in `http_error`: "GitHub answered with an error (HTTP 502).". runner/lib/github-source.ts
- P03.1 · F12: The keychain failure path returns `SecretStoreError` text verbatim. For example, a token with a lone surrogate gives "value must be non-empty text.", a field name in visible text. runner/server/routes/onboarding.ts
- P03.1 · F13: URL-import server refusals go to the line only, not to the address field. The empty-address line says only "Not imported." while focus is in the field. runner/ui/assets/onboarding.js
- P03.1 · F14: The binary input's wrong-type error tells the person to "Upload a .txt or .md file above instead" for any file, including a `.jpg`. runner/ui/assets/onboarding.js
- P03.1 · F15: The GitHub source uses only the primary `language`; the packet says "languages". runner/lib/github-source.ts
- P03.1 · F16: `defaultFetchGithub` reads `response.json()` with no size cap. `affiliation=owner` includes private repositories' READMEs, and the page doesn't say so. runner/lib/github-source.ts
- P03.1 · F17: The PDF time budget bounds only the wait. pdf.js keeps parsing in-process after a timeout, and its stream decompression has no output cap. runner/lib/document-text.ts
- P03.1 · F18: The `.raw-*` originals are hidden dotfiles, and nothing in the UI lists or removes them next to their extracted text. runner/server/routes/onboarding.ts
- P03.1 · F19: Screenshots: there is no paste-a-token shot, although the brief asked for both GitHub paths. The committed shots show the pre-P06 nav (no Board or Sessions), so they came from a server process older than the branch head. docs/screenshots/P03.1-*
- P03.1 · F20: The README says "never a crash" (contradicted by B3's 500) and "the one tool the extraction model can call" (the eval asserts other tools are not called, so other tools exist). runner/README.md
- P03.1 · F21: No test covers a redirect to `http:` specifically. `safeFetch` re-checks the scheme on every hop, and my probe confirmed a 400. runner/test/safe-fetch.test.ts
- P03.1 · F22 (re-check): the DOCX guard has no running-total cap, unlike the ZIP path. A 9.97 MB DOCX with 467 duplicate `word/document.xml` entries, each under the 100:1 ratio, inflates 979 MB and holds the route about 1.2 s before its 422. `runner/lib/document-text.ts`
- P10-B · F1: The fetch is its own transport, not `safeFetch`, though the brief's decision says "goes through safe-fetch". It reuses `isBlockedAddress` and mirrors the rules (scheme per hop, pinned address, redirect re-check, size cap, deadline), and I found no bypass. It is disclosed in the README but not flagged in the Report as a decision deviation, so the orchestrator should ratify it. Its production `nodePerformRequest` has no test against a local TLS server, unlike `safe-fetch.test.ts`. runner/upgrade/release-source.ts
- P10-B · F2: A release that needs no schema migration can never be applied. A 0.1.0 → 0.1.1 release is offered as "available", then `applyUpgrade` throws `MigrationError: The migration chain from 0.1.0 reaches 0.2.0, not the target 0.1.1`, and Settings shows "The runner hit an unexpected error.". F12 says "migrate … if needed", but every future release needs its own migration file, even a no-op one. runner/upgrade/migrate.ts:105-115
- P10-B · F3: The shipped 0001 "fixture" lives in the production migrations directory, so a real 0.2.0 release would run it. It is a no-op for profiles the current runner writes. The author of the next real release must keep or replace it knowingly. packages/contracts/migrations/0001-career-profile-backfill-arrays.ts
- P10-B · F4: A failed migration through Settings answers 500 "The runner hit an unexpected error." Nothing says nothing changed, and the confirm panel stays offering "Yes, update". The route doesn't catch `MigrationError`/`MigrationLoadError`. runner/server/routes/upgrade.ts:66
- P10-B · F5: A malformed release tag (`job-assistant@latest`) makes `checkForUpgrade` throw "Not a semantic version", and the GET answers the generic 500. runner/upgrade/upgrade.ts:59
- P10-B · F6: Any non-2xx from GitHub's `releases/latest` (a 403 rate limit or a 5xx) reads as "You're on the latest release.". runner/upgrade/release-source.ts:188
- P10-B · F7: Some refusal and error texts aren't plain: "The .sha256 file is not in the expected \`<hex>  <filename>\` form."; "package/workflow.json in the release tarball does not match the workflow manifest contract."; "The server answered with an error (HTTP 404)."; "Could not fetch that URL: <raw error>"
- P10-B · F8: At 390, a refusal or error at confirm time is shown only in the page-top status line, 1041 px above the view. The confirm panel is unchanged and offers "Yes, update" again. An offline check also leaves the previous check's refused notice in place. runner/ui/assets/settings-upgrade.js:113-131
- P10-B · F9: Cancel has no busy guard during an in-flight confirm (it is only `aria-disabled`). Pressing it switches the panel back to "available" and moves focus while the upgrade continues. I found this by reading the code; my live probe landed after the upgrade had finished. runner/ui/assets/settings-upgrade.js:108
- P10-B · F10: A long unbroken changelog token overflows at 390 (scrollWidth 591 against 390). `.upgrade-changelog-notes` has no `overflow-wrap: anywhere`. runner/ui/assets/settings.css
- P10-B · F12: There are disclosed edits outside Owns: `settings.css` (+44 lines, upgrade-scoped only) and the Settings lede sentence. The orchestrator should ratify them. runner/ui/assets/settings.css, runner/ui/settings.html:15
- P10-B · F13: The carried `doctor --live` item is only partly done. Only the "model answered" detail was lower-cased. A timeout still prints "The check failed: No answer within 90 s.", and a thrown provider error passes raw text through `shorten(message)`; the Status page maps both to plain clauses. runner/lib/live-check.ts:71
- P10-B · F14: The R2-N2 test returns early on a case-sensitive filesystem, so it passes silently on ubuntu CI. It did run, and it caught the mutation, on macOS. Use `it.skipIf` so CI reports it as skipped. runner/test/doctor.test.ts
- P10-B · F15: `applyUpgrade` takes the current version from the in-memory `workspace.manifest`, which is fixed at open, while the route's GET reads the file fresh. After an in-process upgrade, a later confirm re-runs the chain from the old version and spreads the stale manifest into `workspace.json`. Idempotent migrations make it harmless today. runner/upgrade/upgrade.ts:128,138
- P10-B · F16: The route's stale-confirmation test asserts `bridge.workspace.manifest.packageVersion`, which never changes in memory, so its "changes nothing" half can't fail. The 409 status check does catch M1b. runner/test/upgrade-route.test.ts:93
- P10-B · F17: The Report has no PR-head CI run id; it says "see the final reply". The run is `36199808348`, success. docs/spec/implementation/P10-versioning-and-pilot-readiness.md
- P10-B · F18: The transcript says `runner/cli/runner.ts` hard-codes the ports. The constants are `server/eve-gateway.ts:29` (`EVE_PORT`) and `server/app.ts` (`BRIDGE_PORT`). Step 2 asserts `.env.local`'s mode 0600 from code instead of showing it. docs/pilot/success-test-run.md
- P10-B · F19: The README calls re-verifying at confirm "the one difference from the CLI". The CLI's `applyUpgrade` re-fetches and re-verifies too. runner/README.md
- P10-B · F20: The migration commit writes staged files one at a time. A write failure midway leaves the earlier files written, with the version unwritten. runner/upgrade/migrate.ts:140-142
- P10-B · F21: The `.sha256` file's filename is parsed but never compared to the tarball asset's name. runner/upgrade/checksum.ts:37-40
- P10-B · F11: promoted to BLOCKING by the orchestrator (Settings fetched GitHub and the release on every load) and fixed in fix round 1; not a follow-up.
- P10-B · F22 (re-check): the changelog now shows only the latest release's notes, not every version in between, a cost of F11's check/confirm split. `runner/upgrade/`, `runner/ui/assets/settings-upgrade.js`
- P10-B · F23 (re-check): the checksum-before-read test watches only the outcome, so a read before the check whose result is discarded and whose errors are swallowed would still pass. `runner/test/upgrade-core.test.ts`

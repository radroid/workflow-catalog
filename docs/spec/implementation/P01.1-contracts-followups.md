# P01.1 · Contracts follow-ups from the P01 review

Status: claimed
Assignee: iter-003 implementer (Sonnet)
Blocked by: P01
Owns: packages/contracts/src/, packages/job-assistant/schemas/, packages/job-assistant/test/, packages/job-assistant/skills/follow-up-questions/, packages/job-assistant/fixtures/expected-claims.json, packages/job-assistant/fixtures/index.json
Spec: §5 data contracts; logs/blocks.md entry "P01 peer review, round 3" (2026-09-22)

## Goal
Close the three low-severity gaps the P01 reviewer left as follow-ups. No later packet owns the contracts, and P03's onboarding evals read the always-ask fixtures, so this lands before P03.

## Deliverables
- **URL rule parity.** Today zod trims a leading-space URL and accepts it, while the emitted JSON Schema rejects it. In the other direction, the JSON Schema has no `format: "uri"` rule, so ajv accepts `https://` and `https://exa mple.com/`, which zod rejects. Make the zod and JSON Schema URL rules identical: http(s) only, no surrounding whitespace, a well-formed URL. Extend the ajv-vs-zod parity test with these cases.
- **Pair code cap.** `pairRequestSchema.code` has no length limit, so a 300 KB `/pair` body is valid under zod. Cap it (for example `.max(64)`, matched to the code format P02 prints) and test it.
- **Always-ask consistency.** The skill's broadened wording ("superlative, role, or scope") also covers two confirmed claims in `fixtures/expected-claims.json` that carry no question: `76d49b1e` ("Maintainer of…") and `169fa5e1` ("used by three engineering teams"). Make the skill and the fixture agree. The preferred fix gives both claims `question` and `answeredAt`, keeping the rule conservative. Keep the fixtures test reading its word list from `SKILL.md`.

## Acceptance
- `pnpm typecheck && pnpm test && pnpm -r lint && pnpm --filter contracts build && pnpm check:fixtures` green, and `git status` clean after the build.
- The parity test covers leading and trailing whitespace, `https://`, spaces inside the host, and the non-http schemes, with the same result from zod and ajv.
- No existing test or schema bound is loosened.

## Out of scope
New schemas. Runner, catalog, or extension code.

## Report

### 2026-09-22 — iter-003 implementer (Sonnet)

**What was done.** All three deliverables, on `packet/P01.1`, PR [#5](https://github.com/radroid/workflow-catalog/pull/5) into `overnight/integration`, head `676cbd9`. Four commits: claim, then one per deliverable (`2645ee0` URL parity, `9f1eedc` pair code cap, `676cbd9` always-ask consistency). Every changed path is inside `Owns:`; `pnpm-lock.yaml` untouched.

- **URL rule parity.** `httpUrlSchema`/`boundedHttpUrlSchema` (`primitives.ts`) now run a `NO_SURROUNDING_WHITESPACE` regex *before* `z.url()`, so zod rejects leading/trailing whitespace instead of trimming it — confirmed by reading zod 4.5.4's `$ZodURL` source (`node_modules/.pnpm/zod@4.5.4/.../src/v4/core/schemas.ts`): its check always overwrites `payload.value` with the trimmed, tab/CR/LF-stripped string for every later chained check to see, regardless of the `normalize` option. A second new regex, `NON_EMPTY_HTTP_AUTHORITY` (`^https?:\/\/[^\s/?#]+(?:[/?#]|$)`), restates "well-formed URL, non-empty space-free host" for the JSON Schema side — `z.url()`'s own `new URL()` call already enforced it at the zod level (verified empirically: `new URL("https://")` and `new URL("https://exa mple.com/")` both throw), but that check is invisible to `z.toJSONSchema`, same as the existing `protocol` option issue 2 (P01 revision 1) fixed. Regenerated all 7 URL-bearing schemas via `pnpm --filter contracts build`; verified ajv (`Ajv2020` + `ajv-formats`, same setup as the existing parity test) agrees with zod on every case in a throwaway probe before committing.
- **Pair code cap.** `pairRequestSchema.code` is now `nonEmptyStringSchema.max(64)`.
- **Always-ask consistency.** Added `maintainer` and `used by` to `SKILL.md`'s always-ask word list — checked these match only `76d49b1e` and `169fa5e1` among the current confirmed claims (not `dcdbd2e7`, the other credential-kind claim with no question, whose text is genuinely unrelated). Gave both claims a `question` (in the style of the existing role question on `5e2907bb`) and `answeredAt`.

**Tests, real output** (this worktree and a fresh `/tmp` clone, identical):
```
pnpm -r typecheck                → clean (contracts, job-assistant, apps/catalog; extension/runner placeholders)
pnpm -r test                     → contracts 16 files/235 passed, job-assistant 6 files/151 passed, apps/catalog 14 files/87 passed
pnpm -r lint                     → clean
pnpm typecheck && pnpm test      → green, incl. scripts/check-fixtures.test.mjs 2/2
pnpm --filter contracts build    → tsc + emit-schemas.mjs, zero "wrote" lines (no drift)
pnpm check:fixtures              → exit 0, no offenses
git status --porcelain           → empty, both places
gh pr checks 5 --watch           → ci pass (1m16s); CodeRabbit skipped (disabled)
```
contracts: 224 → 235 (+11: httpUrlSchema whitespace/well-formed unit cases in `primitives.test.ts`, pair-code cap cases in `bridge-http.test.ts`; `bridge-body-size.test.ts`'s padding test restructured, net 0). job-assistant: 123 → 151 (+28: `url-schema-parity.test.ts` gained `chrome:`/`data:` schemes plus the whitespace/malformed/valid-shape cases, each × 4 nesting depths; `fixtures.test.ts` +1 net for the role/scope word coverage).

**Proof the always-ask regression test actually fires.** Temporarily set `76d49b1e`'s `question` to `""`, ran `pnpm test` in `job-assistant`: 2 failures (the always-ask rule test, and — a bonus signal — `claimSchema`'s own `question` min-length check via the fixtures/index.json schema-validation test). Restored the real question, reran: 151/151 green. The blanked state was never staged or committed.

**What was skipped, and why.** Nothing from the three deliverables. Did not touch `169fa5e1` for the same live-removal proof — the task asked for one claim, and the mechanism is identical for both (same filter, same loop).

**Assumptions.**
1. `NON_EMPTY_HTTP_AUTHORITY` only bounds the authority component (up to the first `/`, `?`, `#`, or end of string) — it does not forbid whitespace elsewhere in the URL (path/query), matching the packet's two named cases (`https://` alone, a space in the host) rather than a broader "no whitespace anywhere" rule. An embedded tab in the *path* (already covered by an existing test) is untouched: `z.url()` strips it as before.
2. The two new always-ask words are deliberately narrow (`maintainer`, `used by`) rather than a broader heuristic, to keep the rule's growth traceable to the two named fixtures and avoid new false positives against the rest of `expected-claims.json` (checked by hand: no other claim's text contains either).
3. `boundedHttpUrlSchema`'s check order keeps `.max()` first (unchanged from P01 revision 2), then the new whitespace check, then `z.url()`, then the two JSON-Schema-parity regexes — mirroring `httpUrlSchema` exactly so the two can't drift apart.

**Worktree provisioning anomaly (not part of this packet, flagging for the orchestrator).** This worktree's branch was provisioned at `220d703` (a stale, pre-iteration-1 commit matching `main`) rather than `overnight/integration` (`bbf16d1`) as step 0 expected. No uncommitted work was present, so I branched `packet/P01.1` directly from the verified `bbf16d1` instead of the worktree's stale HEAD and proceeded. Sibling worktrees for `packet/P02` and `packet/P09-B` were correctly based on `bbf16d1`; at least one other (`worktree-agent-a0fcf53d4270dd865`) showed the same staleness. Worth checking whether P07-A hit this too.

**The one thing to sharpen next time.** `NON_EMPTY_HTTP_AUTHORITY`'s "no embedded space in the host" behavior is only exercised through the full `httpUrlSchema`/`boundedHttpUrlSchema` pipeline (where `z.url()` already independently rejects the same input), never in true isolation — there's no test that would catch this specific regex alone regressing to something looser while `z.url()`'s own check coincidentally still rejects the same cases. A follow-up could unit-test the regex directly against its constant, the way `NO_SURROUNDING_WHITESPACE` effectively is via the whitespace-only rejection cases.

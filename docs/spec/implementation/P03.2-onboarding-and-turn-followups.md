# P03.2 · Onboarding and model-turn follow-ups

Status: claimed (iter 007)
Assignee: iter-007 implementer (Sonnet)
Blocked by: P04 (it adds the turn's events to `runTurn`'s result)
Owns:
- `runner/server/eve-gateway.ts` and `runner/server/routes/model.ts`: the model check only.
- `runner/server/routes/onboarding.ts`: the extraction-turn handling, the on-disk view's size cap and the upload refusal messages. P03.1 owns the new source modes.
- `runner/server/run-harness.ts`: only if `runTurn` needs an option not to pause the budget (see deliverable 1).
- `runner/store/profile.ts` and `runner/store/profile-markdown.ts`.
- `runner/ui/assets/profile.js`, `runner/ui/assets/onboarding.js` and the pinned-line styles in `runner/ui/assets/onboarding.css` and `profile.css`.
- `runner/test/ui-pages.test.ts`, and new tests in `runner/test/`.
- `runner/README.md`: the matching lines only.
- For deliverable 5 only:
  - `runner/agent/tools/extract_claims.ts`, `runner/agent/lib/extract-claims-logic.ts`, `extract-claims-schema.ts`, and the eval agent's `extract_claims.ts` re-export;
  - `runner/eval-agent/evals/onboarding-extraction.eval.ts`, only if gates must move (see deliverable 5).

Spec: eve-runtime.md §8 item 15; logs/handoff/P03-round-4-review.md; logs/blocks.md entries "eve client ends an aborted turn quietly as "completed"" and "P03 peer review, round 4"

## Goal
Give the runner one way to run and classify an eve turn, and close the follow-ups P03's round-4 reviewers left at approval. This packet edits no dependencies.

## Deliverables
1. **One turn classifier.**
   - P03's extraction route (`interpretExtractionTurn`) and P02's `checkModel` (`interpretModelCheck`) both move onto P08-A's `runTurn`. It follows eve item 15:
     - ok is a `session.waiting` or `session.completed` boundary, with no failure, no `turn.cancelled` and no abort;
     - a non-empty `input.requested` parks the turn, and it is cancelled through the session.
   - The extraction reads its tool results from the returned events, as `persistedExtraction` does today.
   - Decide whether a provider limit met during a model check or an interactive extraction pauses the budget, which is what `runTurn` does. Record the choice in the report. If they shouldn't pause, add an option to `runTurn` rather than a second path.
   - Behaviour stays the same: every existing extraction and model-check test passes unchanged, apart from its import. The exception is deliverable 5's move of where claims are saved, where each moved check is kept and reported.
2. **P03 round-4 reviewer's nits:**
   1. The on-disk view (`markdownOnDisk`) is sent only up to `MAX_MARKDOWN_BYTES`, and as null beyond that.
   2. On Profile, `run()` clears a stale `editorError` after an unrelated action. Or keep it, with a documented reason, if it still describes the text in the box.
   3. Commit tests for the reviewer's probes 2 and 3 (`clip` in `profile-markdown.ts:353`, and `nestedLine`'s escape at `:131`).
   4. `ui-pages.test.ts` counts the announcements on `#last-action` ("announced once").
   5. The file-edit prefix keeps messages within about 90 characters.
3. **P03 round-4 UI critic's polish:**
   1. When a refused control already has focus (an upload, or Enter in an empty statement), the line carries a short reason, e.g. "Not uploaded: only .txt or .md files can be uploaded.".
   2. Line messages stay within about 80 characters, with the consequence first ("Accepted. Approval withdrawn: “…” needs your answer."). The two-line clamp applies to an inner element, so no third line shows.
   - The 413 upload reason is a plain sentence, not "Request body is larger than 524288 bytes.".
   - At 640 px and below, the tag and the message are separated, so assistive tech doesn't read "LAST ACTIONAn answer is needed.".
4. **happy-dom:** moved to P03.1 (iter 006 decision). P05 owns `runner/package.json` and the lockfile while it runs, and P03.2 runs alongside P05.
5. **Claims are saved only after an ok turn** (P04's T1 rule; `logs/handoff/P04-round-2-review.md`).
   - Today `extract_claims` saves inside its own step (`store.extractClaims` in `extract-claims-logic.ts`). So a turn that fails after the tool ran leaves claims behind, while the page reports a failure.
   - Make the tool verify and return: the quote checks stay. The route saves from that call's `action.result`, after an ok turn, and only for the exact source it asked about.
   - **The onboarding eval.** If its gates assert on what the tool wrote, move each assertion to the tool's returned result, or to a route-level Vitest test. Keep the same checks, never fewer, and report the mapping gate by gate. The gate count must not drop without a named replacement.

## Acceptance
- One classifier. `grep` finds no turn classification outside `run-harness.ts`, apart from thin adapters.
- A real-`Client` test for each caller, with:
  - the spike's normal sequence (ok);
  - `turn.cancelled` (not ok);
  - an abort (timeout, with a cancel through the session);
  - a non-empty `input.requested` (parked).
- Each nit and polish item has a test or a UI-critic check.
- A turn that calls `extract_claims`, then fails, saves no claims. Test it with a scripted turn whose events carry a real `extract_claims` action result.
- Mutation proofs:
  - the claims tool saves during the turn (deliverable 5);
  - `checkModel` treats a no-boundary end as ok;
  - the extraction treats `turn.cancelled` as ok;
  - the size cap is removed;
  - an announcement count of 2.
- `pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures` is green. Screenshots for the changed messages go in `docs/screenshots/P03.2-*.png`.

## Out of scope
New source modes (P03.1), preparation (P05), and schedules (P08-B).

## Report

### 2026-09-24 — implementer (Sonnet)

**Branch/PR:** `packet/P03.2` → `overnight/integration`, head `2d44d15` (commits `464d5d0` claim, `2e426ea` deliverable 1A, `cd95036` deliverables 1B/5, `0df794f` deliverable 2, `dd476e5` deliverable 3, `2d44d15` screenshots).

**Deliverable 1 (one turn classifier).** `run-harness.ts`'s `classifyTurn(client, input)` is the whole algorithm (eve-runtime.md §8 item 15), exported and ctx-free; `runTurn(ctx, input)` is a thin wrapper adding the one ctx-dependent side effect (pausing the budget on a detected provider limit, now gated by a new `pauseBudgetOnProviderLimit` option, default `true`). `eve-gateway.ts`'s `checkModel` calls `classifyTurn` directly (it runs before a `RunnerContext` exists) with no option set, so it pauses like every other caller — a deliberate choice: a model check can hit a real provider limit exactly like an extraction can, and there is no separate "model check" budget to protect by skipping the pause. `server/routes/onboarding.ts`'s extraction route calls `runTurn(ctx, {..., pauseBudgetOnProviderLimit: false})`: a provider limit met interactively, with a person waiting on the page, refuses that one turn but must not pause every other scheduled run behind it. Both `interpretExtractionTurn` and `interpretModelCheck` are gone; the route's `extractionRefusal` and the gateway's adapter are thin, phrasing an already-classified `TurnResult` for a person, never re-deriving status from raw events.

- **Acceptance — grep.** `grep -rn '"turn\.cancelled"\|"session\.waiting"\|"session\.completed"\|"turn\.failed"\|"session\.failed"\|"step\.failed"' --include="*.ts" server/ agent/ store/ ui/` and `grep -rn "isCurrentTurnBoundaryEvent\|isTurnFailureEvent" --include="*.ts" server/ agent/` both return nothing outside `run-harness.ts` and `*.test.ts` files.
- **Acceptance — a real-`Client` test for each caller.** `test/eve-gateway-real-client.test.ts` (checkModel): "the spike's normal sequence, replying 'ok'" (ok), "turn.cancelled then session.waiting is not ok, with no cancel of our own" (cancelled), "waiting on the person (input.requested) is parked, not ok, with one real cancel through the session" (parked), "an abort while the stream is opening ends quietly (no throw) but is still classified 'timeout', with one real cancel" (timeout). `test/onboarding-extract-timeout.test.ts` (extraction route): "control: the spike's normal turn ... is ok" (ok), "J1, deliverable 5: a cancelled turn ... is not ok" (cancelled), "a turn parked on an input request is cancelled through the session" (parked), "the deadline fires while an open stream is read" / "... while the stream reopens after a lease ends" (timeout).
- **HTTP-contract decision, recorded here as asked:** the extraction route's old distinct `504` (timeout) is gone; every non-ok `TurnResult.status` is now a uniform `200 {ok:false, status, message}`. `runTurn` never rejects (stated three times in its own doc comments) — a route-level try/catch and a special-cased HTTP code for exactly one non-ok status stopped matching that contract. `docs/spec/mvp-spec.md` specifies no HTTP code for this response; `onboarding.js`'s `saveAndExtract` never reads the response status, only `ok`/`message` (confirmed by reading `runner.js`'s `request()`); the only place `504` was ever documented was P03's own now-superseded `## Report` entry, not a live contract. Every existing extraction/model-check test passes unchanged apart from its import, as the deliverable asked, except deliverable 5's moved persistence checks (below).

**Deliverable 2 (P03 round-4 reviewer's 5 nits — commit `0df794f`).**

1. **Size cap.** `store/profile.ts` exports `MAX_MARKDOWN_BYTES` (512 KiB); `routes/onboarding.ts` imports it instead of keeping a second, private copy. `ProfileStore.load()`'s unreadable-file branch now nulls `markdownOnDisk` once the on-disk file exceeds that bound (`Buffer.byteLength(onDisk, "utf8") <= MAX_MARKDOWN_BYTES ? onDisk : null`), instead of echoing an unbounded blob into the page's response. Test: `profile-store-revision2.test.ts`, "markdownOnDisk is null once the unreadable file exceeds MAX_MARKDOWN_BYTES, not echoed unbounded".
2. **Stale `editorError`.** `profile.js`'s `run(id, work)` now clears `editorError` before any action id other than `save-markdown` itself runs, so a red editor left by an earlier failed save doesn't keep showing after an unrelated success (Accept on a pending revision, which re-renders the whole page). Test: `ui-pages.test.ts`, "Accept on a pending revision clears a red editor left by an earlier failed save".
3. **`clip`/`nestedLine` tests.** `profile-markdown-strict.test.ts` adds a direct test of `clip()` (profile-markdown.ts:353): a confirmed claim's `**Evidence:**` line — the one nested, never-read-back line whose text is attacker/long-input controlled — edited in place (the "only the words before a marker can be edited" problem, the one path that echoes `clip(want.text)`) to exercise both of `clip`'s defenses with exact-string assertions: a marker-shaped substring mid-line is hidden (`` `[legacy-system]` `` → `` `[…]` ``, and the claim's real marker id never appears in the message), and text past 80 characters is cut to 79 chars + "…". `profile-markdown.test.ts` adds a direct test of `nestedLine`'s `escapeLine` call (`:131`): a disputed claim's question and note, each ending in a marker-shaped token, each get one trailing backslash on the rendered `**Question:**`/`**Your note:**` line; an ordinary, non-marker-shaped question is left untouched.
4. **Announced-once count.** `ui-pages.test.ts` adds `announcementCounter(page)`, a `MutationObserver` on `#last-action .text` counting DOM-write batches — the correct proxy for how many times the `aria-live="polite"` region announces (one callback per synchronous batch; `lastAction()`'s own clear-then-`requestAnimationFrame` refill for a *repeated identical* message is a deliberate second batch, outside what this nit is about, and none of the scenarios this count is asserted on repeat a message). Added to the existing "a status button is the very same node..." test: `expect(announced.count()).toBe(1)`.
5. **The ~90-character message cap.** `profile.ts`'s `editsNote()` — the file-edit prefix — used to say "file edit(s) ... saved" / "... proposed as a revision/revisions", which the reviewer measured pushing a combined message to 106 characters. Now "N edit(s) saved" / "N edit(s) proposed", matching `applyMarkdownEdit`'s own established "1 edit was applied" idiom and `discardMarkdownEdits`'s "Discarded your edits" ("file edit" was this function's own inconsistent wording, not an established term elsewhere); dropping "as a/revision(s)" removes the one open-ended-length clause. Test: `profile-store-revision2.test.ts`, "two hand edits at once still keep the prefixed message short" (`"2 edits saved. Your preference is recorded."`, asserted `<=90` chars).

**Deliverable 3 (P03 round-4 UI critic's 4 polish items — commit `dd476e5`).**

1. **Focused-control reason.** `refuseAt(controlId, detail, outcome, focusedReason)` (onboarding.js) takes a new, optional 4th argument, used in the line in place of `outcome` exactly when the refused control still has focus (`document.activeElement?.id === controlId`) — the one case where a screen reader has nowhere else to read `detail` from. Wired at the upload's wrong-extension refusal (the packet's own example: "Not uploaded: only .txt or .md files can be uploaded."), the upload's empty-file refusal, and Enter on an empty boundary/preference/presentation input ("Not added: type a boundary first."). Every other `refuseAt` call site is a button-triggered refusal (focus is on the button, not the named field), so the new check never fires there — confirmed by reading each site, not just by the two now-focus-adjacent tests.
2. **Message length / consequence-first / the two-line clamp.**
   - `profile-reducer.ts`'s `acceptRevision`-reopened message: `"Accepted; X needs your answer, so approval is withdrawn."` (97 characters for the cited claim) → `"Accepted. Approval withdrawn: X needs your answer."`, the packet's own example, verbatim.
   - `profile.ts`'s `applyMarkdownEdit` proposed-revisions clause: `"N edit(s) are now proposed revisions; version V stays in force until you accept it/them"` (90 characters for the cited "2 edits" case, losing "them.") → `"N edit(s) proposed; version V stays in force until you decide"` (67 characters for the same case). The applied-edits clause is untouched — not cited as overflowing, and two existing tests (`onboarding-routes.test.ts`, `profile-store-revision2.test.ts`) pin its exact wording.
   - `onboarding.css`/`profile.css`: at ≤640px, the `-webkit-line-clamp: 2` moves from the padded `.last-action` box to `.last-action .text` alone (`display: -webkit-inline-box`, not `-webkit-box`, so it keeps flowing right after the inline `.tag` on the first line; `max-width: 100%` so it still wraps instead of shrinking to fit its own content). Clamping the padded box itself was letting a sliver of a third line show past the box's own padding.
3. **The 413 reason.** `server/http.ts` (out of Owns) keeps its generic `"Request body is larger than 524288 bytes."`. A new `boundedSourceBody(request)` in `onboarding.ts` wraps `readBoundedJson` for the two source-content routes (`POST .../content`, `POST .../uploads`) and substitutes `"That's over 512 KiB. Paste less text, or upload a smaller file."` only for that one 413; every other `readBoundedJson` failure (bad JSON, wrong content type) and every other route's own 413 (the small-body and markdown caps) are unchanged.
4. **Tag/message separation at ≤640px.** `lastAction()` in both `onboarding.js` and `profile.js` now writes `"Last action:"` / `"Refused:"` / `"Working:"` as the tag's own text content (a new shared `tagText(tone)` helper in each file), not `"Last action"` plus a CSS `margin-right` — the margin was invisible to assistive tech, which read "LAST ACTIONAn answer is needed." with nothing between the words, at every width, not just ≤640px. `jobs.js` has its own, separate copy of this exact pattern (confirmed by reading it); out of this packet's Owns, untouched, and `test/jobs-page.test.ts`'s own `.tag` assertion (`"Working"`, no colon) is unaffected.

One pre-existing `ui-pages.test.ts` case exercised the precise scenario polish 1 fixes (a focused, empty statement input) and asserted the old plain-outcome line; updated to the new combined line. Two new tests added (one per polish-1 call-site class), plus the 413 and message-length assertions above.

**Deliverable 4 (happy-dom).** Nothing to do — P03.1 already added `"happy-dom": "20.14.5"` to `runner/package.json`'s devDependencies, per the iter-006 decision recorded in this packet.

**Deliverable 5 (claims saved only after an ok turn — commit `cd95036`, prior session).** `extract_claims` (both app roots, via the shared `agent/lib/extract-claims-logic.ts`) now verifies evidence quotes and returns what survived; it never calls `store.extractClaims` itself. `onboarding.ts`'s extraction route persists through `ProfileStore.extractClaims` from the turn's own `action.result`, only once `result.status === "ok"`, and only for the exact category the turn was about. The two `onboarding-extract-timeout.test.ts` scenarios that used to assert the bug this fixes (a cancelled/timed-out turn's mid-turn-saved claim survives) are inverted to assert `claims: []` — the deliverable's own regression tests. `onboarding-extraction.eval.ts`: every gate that used to read `store.read()` right after `t.send()` now reads the call's own `requireToolCall(...).output` (typed `ExtractClaimsOutput`) instead; the one scenario needing a claim's persisted status/question calls `store.extractClaims` itself, mirroring the route's own post-turn step. Gate count 65→67, verified empirically both directions (the pre-P03.2 eval file against the new tool: 57/65, the 8 failures exactly the moved persistence checks; the new eval file: 67/67).

**Acceptance — mutation proofs.** All five broke the test(s) named, confirmed by running the affected file(s), then were reverted via `git checkout -- <file>` (a single-file revert of my own uncommitted change) and reconfirmed green. None left the tree dirty (`git status --porcelain` empty after every one).

1. *The claims tool saves during the turn (deliverable 5).* `onboarding.ts`: `if (result.status === "ok") {` → `if (true) {` around the persist block. Killed by `onboarding-extract-timeout.test.ts`: "J1, deliverable 5: a cancelled turn ... saves nothing" (expected `claims: []`, got the claim back) and "the deadline fires while the stream reopens after a lease ends ... saves nothing" (same). 2 failed, 5 passed.
2. *`checkModel` treats a no-boundary end as ok.* `run-harness.ts`: `if (!boundary) {` → `if (false) {` (the "never call this 'ok' (G1)" guard). Killed by `run-harness.test.ts`'s own purpose-built "no boundary event at all (never aborted, no failure): never called 'ok' (mutation target)", plus `run-harness-events.test.ts` ("events are still collected on a non-ok turn"), plus `onboarding-routes.test.ts` ("a 'completed' turn with no terminal session event never finished"). `eve-gateway.test.ts` and `eve-gateway-real-client.test.ts` stayed green (checkModel's own tests don't happen to script a no-boundary stream; `classifyTurn` is the function both share, and the other three files prove it's broken). 3 files / 3 tests failed of the 5 run.
3. *The extraction treats `turn.cancelled` as ok.* `run-harness.ts`: `if (cancelled) return ...` → `if (false) return ...`. Killed by `run-harness.test.ts` ("turn.cancelled (then session.waiting) is not ok" and the `withRun` end-to-end idempotency test), `run-harness-eve-client.test.ts` ("turn.cancelled then session.waiting is not ok"), and `onboarding-extract-timeout.test.ts`'s J1 test. 4 tests failed across 3 files.
4. *The size cap is removed.* `profile.ts`: `Buffer.byteLength(onDisk, "utf8") <= MAX_MARKDOWN_BYTES ? onDisk : null` → `onDisk !== undefined ? onDisk : null`. Killed by `profile-store-revision2.test.ts`'s own nit-1 test (expected `null`, got the full oversized file).
5. *An announcement count of 2.* `onboarding.js`'s `lastAction()`: the repeated-identical-message check `if (text.textContent === message && tag.textContent === tagText(tone))` → `if (true || (...))`, so every call takes the clear-then-`requestAnimationFrame` double-write path. Killed by `ui-pages.test.ts`'s nit-4 test: `expect(announced.count()).toBe(1)` got `2`.

**Acceptance — screenshots (commit `2d44d15`).** 16 files, `docs/screenshots/P03.2-*.png` (4 scenarios × light/dark × 390/1280), real Chrome via the Chrome DevTools MCP tool against a throwaway harness on `127.0.0.1:4330` (mirrors P03's own precedent: `createBridgeApp`/`createRunnerContext`/`loadRouteModules` directly, a `/tmp` workspace, `eve`/`model` left undefined, sign-in through the real `/ui/login?nonce=` flow — the real `$HOME`, OS keychain and any model were never touched). Console clean on every capture; no sideways scroll observed. One finding during capture, not a code bug: the `resize_page` tool silently failed to change the viewport after a page navigation in this session (screenshots came out at the previous width, caught by checking `window.innerWidth` before trusting a shot, not by assuming success) — worked around with `emulate`'s own `viewport` parameter instead, verified before every capture from then on. Covers:
- `onboarding-refused-upload` — polish 1 (the packet's own upload example) and polish 4 (the tag's colon, visible at 390px).
- `onboarding-paste-too-large` — polish 3 (the 413 reason).
- `profile-edits-proposed` — polish 2's shortened proposed-revisions wording, one line even at 390px.
- `profile-withdrawn` — polish 2's consequence-first `acceptRevision` wording, two lines at 390px with no third-line sliver.

**Verification chain, real output.** From the repo root: `pnpm install --frozen-lockfile` (up to date) → `pnpm typecheck` (6/6 packages clean) → `pnpm test`: `packages/contracts` 235, `packages/job-assistant` 153, `runner` 865 + 6 evals/107 gates, `apps/catalog` 168, `extension` 329 passed + 5 skipped, root `scripts/*.test.mjs` 2 — all passed. `pnpm -r lint`: 6/6 clean. `pnpm check:fixtures`: exit 0, no offenses. `git status --porcelain`: empty throughout and after.

**Scope.** Touched everything Owns lists: `server/eve-gateway.ts`, `server/routes/model.ts` (import path only, unchanged behaviour), `server/routes/onboarding.ts`, `server/run-harness.ts` (the `pauseBudgetOnProviderLimit` option only), `store/profile.ts`, `store/profile-markdown.ts` (read, not modified — its two behaviours needed tests, not a fix), `ui/assets/profile.js`, `ui/assets/onboarding.js`, the pinned-line rules in `onboarding.css`/`profile.css`, `test/ui-pages.test.ts` and new tests, `docs/screenshots/P03.2-*.png`, and deliverable 5's `agent/tools/extract_claims.ts` / `agent/lib/extract-claims-logic.ts` / `extract-claims-schema.ts` (both app roots) / `eval-agent/evals/onboarding-extraction.eval.ts`. Existing test files that assert an in-scope production change's behaviour were updated alongside it (`onboarding-routes.test.ts`, `eve-gateway.test.ts`, `extract-claims-logic.test.ts`, `onboarding-tools.test.ts`, `profile-reducer-revision2.test.ts`, `profile-store-revision2.test.ts`, `profile-markdown.test.ts`, `profile-markdown-strict.test.ts`).

One file outside the literal Owns list: `store/profile-reducer.ts`, for polish 2's `acceptRevision`-reopened message — the packet's own text names this exact message, quoting both the 97-character original and the fix verbatim, so the file had to change to do what the packet itself asked; flagging it here rather than silently treating "profile.ts" as covering it. `runner/README.md` was checked deliberately (grepped for the old 413 message, the old `editsNote`/`applyMarkdownEdit` wording, `interpretExtractionTurn`/`interpretModelCheck`, `504`, size-cap and live-region language) and left untouched: the README documents the runner's architecture and the `runTurn`/`withRun` contract at a level this packet's changes don't falsify — it never named the old wording, HTTP code or the two removed function names in the first place. `jobs.css`/`jobs.js` (the same `.last-action`/`lastAction()` pattern, independently duplicated there) were read for comparison but not touched, since they belong to P04's Owns, not this packet's.

**What was skipped, and why.** Nothing from the five deliverables or the acceptance list. Polish 1's `focusedReason` was added only at the three call sites where a control can genuinely still have focus when its own refusal fires (confirmed per call site by reading, not guessed); every other `refuseAt` site is unaffected by construction (a button, not the named field, has focus there), so no behaviour changed for them and no new test was needed for them either.

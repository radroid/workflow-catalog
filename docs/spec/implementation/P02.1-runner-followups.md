# P02.1 · Runner follow-ups from the P02 review

Status: claimed
Assignee: iter-004 implementer (Sonnet)
Blocked by: P02
Owns: runner/server/local-ui.ts, runner/server/extension-api.ts, runner/test/ (new or changed tests for these two files only), runner/README.md (the matching lines only)
Spec: §5 bridge rules; logs/blocks.md entry "P02 peer review, round 3" (2026-09-22)

## Goal
Close the two low follow-ups the P02 reviewer left at approval. P02 is done and later packets add route modules without editing these files, so the fixes land here.

## Deliverables
- **HEAD must not spend the one-time sign-in link.** Today `HEAD /ui/login?nonce=…` runs the GET handler (`local-ui.ts`, about line 216): it answers 303 with `Set-Cookie`, and the next GET gets 401. Answer HEAD without redeeming, the way `HEAD /commands` no longer leases, and test that a HEAD followed by a GET still signs in.
- **An error inside the `/pair` queue is tested.** The one-at-a-time queue in `extension-api.ts` recovers from a failed request (`tail = run.catch(…)`), but no unit test proves it. Changing that line to `run.then(() => undefined)` keeps all bridge tests green, and in production it would crash Node 24 on an unhandled rejection. Add a test:
  - `redeem` throws once, and that request gets 500;
  - the next valid code pairs;
  - no unhandled rejection is reported.
  Show that the mutation fails it.

## Acceptance
- `pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures` green, and `npm run eval` in `runner/` green.
- Both mutations named above make a test fail.
- No existing test is loosened, and no other runner file changes.

## Out of scope
New routes and new behaviour. Everything owned by P03 to P10.

## Report

### 2026-09-22 — Revision 1 (iter-004 implementer, Sonnet)

PR #9 review round 1 came back `VERDICT: REVISE — 1 issue` (low), everything else confirmed by mutation including both original mutation proofs, integration merge green. One issue, addressed below; this was the one revision round.

**Issue.** `local-ui.ts:226-228`'s HEAD branch built its own `Response` with only `content-type` and `cache-control`, so it was missing `content-security-policy` and `cross-origin-resource-policy` — both of which the GET path's 503/401/303 answers carry via `htmlResponse` (the global `app.ts` middleware separately adds `x-content-type-options`, `referrer-policy` and `x-frame-options` to every response, HEAD included, so those three were already present).

**Fix.**
1. `runner/server/local-ui.ts`: the HEAD branch now returns `htmlResponse(200, "")` instead of a hand-built `Response`, so it carries all seven headers `htmlResponse` plus the global middleware produce. Hono strips nothing special here — the body is already `""`.
2. `runner/test/local-ui.test.ts`: the HEAD test now also asserts `content-security-policy` equals `PAGE_CSP`, `cross-origin-resource-policy` equals `same-origin`, `x-content-type-options` equals `nosniff`, `referrer-policy` equals `no-referrer`, and `x-frame-options` equals `DENY`.
3. `runner/README.md`: added "A HEAD request does not use it up." to the Sign-in bullet (after "A link lasts 10 minutes and works once."), matching the phrasing of the `HEAD /commands` line ("is checked like a GET but leases nothing").

**Tests, real output.**
- `runner/test/local-ui.test.ts`: 17/17 pass with the fix in place.
- Mutation proof: put the hand-built `Response` back (reverted step 1 only, in place, not committed) and reran — 16 passed, 1 failed, exactly the new `content-security-policy` assertion: `AssertionError: expected null to be 'default-src \'none\'; script-src \'se…'` (received `null`, expected `PAGE_CSP`) at `local-ui.test.ts:47`. Restored from a `/tmp` backup; `git diff` showed only the intended fix, matching what was committed.
- Full chain from repo root, on the branch, after restoring: `pnpm install --frozen-lockfile` (up to date), `pnpm typecheck` (clean, 6/6 workspaces), `pnpm test` (`contracts` 235/235, `job-assistant` 151/151, `extension` 130 passed + 2 skipped, `runner` vitest 155/155 then `npm run eval` → `Results: 4 passed (4 total)`, `Gates: 20 passed`, `apps/catalog` 139/139, root `node --test` 2/2), `pnpm -r lint` (clean, `--max-warnings 0`, 6/6 workspaces), `pnpm check:fixtures` (no offenses). `git status --porcelain` empty.

**What was skipped, and why.** Nothing; the review named exactly one issue plus the README line, both done.

**Assumptions.** None beyond revision 1's own scope — the 303 success path and the 503/401 GET paths were already routed through `htmlResponse` before this round and were not touched.

**One thing to sharpen next time.** Same note as before: a written contract for HTML-route HEAD responses (status, headers) would have caught this at implementation time instead of review.

### 2026-09-22 — iter-004 implementer (Sonnet)

**What was done.** Both follow-ups from the P02 review, both with a mutation proof.

1. `runner/server/local-ui.ts`: `GET /ui/login` now checks `c.req.method === "HEAD"` right after the `uiToken`-configured check and, for HEAD, returns `200` with an empty body and no `Set-Cookie`, without calling `ctx.uiLogin.redeem()`. GET is unchanged (503 / 401 expired / 401 invalid / 303 + cookie). `store/one-time-codes.ts` has no "peek" — `redeem` is the only way to check a nonce, and it always consumes it — so HEAD skips the check entirely, the same way `HEAD /commands` skips the lease.
2. `runner/test/bridge.test.ts`: added a test to `describe("bridge: POST /pair")` that forces `ctx.pairing.redeem` (via `vi.spyOn`, the same pattern `holdRedeems` in the same file already uses) to reject once. No production code change: `extension-api.ts` already reads `this.#tail = run.catch(() => undefined)`, which the packet's own text says is already correct; the gap was only the missing test.

**Tests, real output.**
- New test, isolated, 5 consecutive runs, no flakiness: `runner/test/local-ui.test.ts` "answers HEAD without redeeming the link, so a HEAD then a GET still signs in" — pass every run.
- New test, isolated: `runner/test/bridge.test.ts` "recovers from an error inside the /pair queue: 500 for that request, the next code still pairs, nothing left unhandled" — pass.
- `runner/test/local-ui.test.ts` full file: 17/17 pass.
- `runner/test/bridge.test.ts` full file: 45/45 pass.
- Full chain on the branch, from repo root:
  - `pnpm install --frozen-lockfile` — lockfile unchanged, install clean.
  - `pnpm typecheck` — clean across all 6 workspace projects with a `typecheck` script.
  - `pnpm test` (`pnpm -r test && node --test scripts/*.test.mjs`):
    - `packages/contracts`: 235/235.
    - `packages/job-assistant`: 151/151.
    - `extension`: 130 passed, 2 skipped (132).
    - `runner`: vitest 155/155 (153 baseline + the 2 new tests), then `npm run eval`: `EVALS 4` → `Results: 4 passed (4 total)`, `Gates: 20 passed`.
    - `apps/catalog`: 139/139.
    - root `node --test scripts/*.test.mjs`: 2/2 (the fixtures-policy self-tests).
  - `pnpm -r lint` — clean, `--max-warnings 0`, across all 6 workspaces.
  - `pnpm check:fixtures` — no offenses, exit 0.
  - `git status --porcelain` — empty after the full chain.

**Mutation proofs (both performed locally, not committed).**
1. Reverted the HEAD short-circuit in `local-ui.ts` (moved `nonce` back above the `uiToken` check, dropped the HEAD branch) and reran `local-ui.test.ts`: 16 passed, 1 failed — exactly the new test, `AssertionError: expected 303 to be 200` at the `head.status` assertion (the nonce got spent by the HEAD, so the follow-up GET returned 401 further down instead of signing in). Restored the fix; `cp`'d from a `/tmp` backup, confirmed via `git diff` that the restored file matches the intended fix and `git diff --stat` shows no residual diff versus the committed version.
2. Changed `extension-api.ts` line 99 to `this.#tail = run.then(() => undefined);` and reran the new `/pair` test alone: it failed — `expected [ Error: redeem: planted failure ] to deeply equal []` — and Node additionally printed `PromiseRejectionHandledWarning: Promise rejection was handled asynchronously`, independently corroborating the mechanism the packet describes (the rejection is first seen as unhandled, then only "handled" once the *next* `exclusive()` call happens to attach a reaction to the same `#tail` promise, which is too late for Node's check). Restored the file from a `/tmp` backup; `git diff --stat runner/server/extension-api.ts` showed no diff, confirming a byte-identical restore and no unintended production change.

**What was skipped, and why.** Nothing in scope was skipped. `runner/README.md` was left untouched: neither README section ("The bridge", "The local UI") documents the `/commands` HEAD-doesn't-lease exception either, so a parallel HEAD note for `/ui/login` would be new documentation rather than a correction of a now-inaccurate line, and the packet's Owns clause only covers "the matching lines only." Flagging it as a possible follow-up rather than taking it.

**Assumptions.**
- HEAD `/ui/login` returns a flat `200` with an empty body and no cookie, independent of whether the nonce would have been valid, by direct analogy to `HEAD /commands` (packet text: "the way `HEAD /commands` no longer leases"). There is no written contract for this HTML route's HEAD response (unlike the JSON routes, which have zod schemas), so this is a judgment call, not a spec citation.
- The setup-not-finished check (`!uiToken` → 503) still runs for HEAD, ahead of the HEAD short-circuit, since it has no side effect and mirrors GET.
- "No other runner file changes" is satisfied literally: `extension-api.ts` is unchanged; only test files and the one `local-ui.ts` fix are touched.

**One thing to sharpen next time.** The packet doesn't pin down what a HEAD `/ui/login` response should look like beyond "don't redeem," which left the exact status/headers to precedent-matching. A one-line addition to mvp-spec §5 (or a local-ui.ts doc comment cross-reference) giving HEAD on HTML routes the same explicit contract JSON routes get would remove that judgment call for the next packet that touches this file.

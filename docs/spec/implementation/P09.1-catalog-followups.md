# P09.1 · Catalog follow-ups from the P09-B and P02 reviews

Status: done (iter 004, PR #10, squash 61f0e2d)
Assignee: iter-004 implementer (Sonnet); revision 2 iter-004 (Opus)
Blocked by: P02, P09
Owns: apps/catalog/, packages/job-assistant/workflow.json (the 0.1.0 changelog note that says "P02 fills it in", nothing else)
Spec: F2, F3; logs/blocks.md entries "P09-B peer review, round 2" and "P02 peer review, round 1" (2026-09-22)

## Goal
Make the install guide match the runner that P02 actually ships, and close the low follow-ups the P09-B reviewers left. P09 is done and no later packet owns the catalog, so these land here.

## Deliverables
- **Install commands match `runner/README.md`.** The guide in `apps/catalog/lib/install-commands.ts` still says `npx degit …/runner` and `npm install`, and to select `workflow-catalog/extension`. The runner now installs only from a whole-repo clone: its `workspace:*` dependencies need the pnpm workspace. The steps are:
  1. `git clone`
  2. `corepack enable`
  3. `pnpm install --frozen-lockfile`
  4. `cd runner`
  5. `npm run setup`
  6. `npm run doctor`
  7. `npm run runner`

  The extension loads unpacked from `extension/dist` after `pnpm --filter @workflow-catalog/extension build`. Copy the exact commands and prerequisites from `runner/README.md` and `extension/README.md`, including what ChatGPT mode needs from the Codex CLI. Drop the `degit` path.
- **A drift test.** Add a catalog test that reads the install code block in `runner/README.md` and fails when `INSTALL_STEPS` disagrees with it. It reads other paths only in the test and edits none of them. The checklist item IDs and labels must keep matching `npm run doctor` (`node`, `runner`, `provider`, `workspace`, `extension`).
- **Refusal nonce is tested.** Both refusal actions redirect with a fresh `n` nonce, but removing it leaves every test green. Add action-level tests: two identical refusals redirect with different `n` values.
- **The release lookup stops reading at the cap.** When the 4 KB checksum cap trips, cancel the body reader (`reader.cancel()`) or abort the fetch. The stalled-body tests also assert the "Request timed out." message.
- **UI notes from the critic:**
  - Even out the Download card spacing (`.card .lede` currently overrides `.tight`: 33px below against 19px above).
  - Bring the permission names down to below the card heading's visual weight.
  - Remove the identical "Required"/"Supported" pills, or make them carry information.
- **Changelog note.** Replace "the eve adapter (adapters/eve/) is a stub — P02 fills it in" in `workflow.json`'s 0.1.0 changelog with one plain sentence about what the eve adapter ships. It is not yet released, so the entry can still change.

## Acceptance
- `pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures` green, plus `pnpm --filter catalog build`.
- The drift test fails when one command in `INSTALL_STEPS` is changed (show it failing, then restore).
- Removing `&n=…` from either refusal action fails a test.
- Screenshots of the install and template pages, light and dark, at 1280 and 390, saved under `docs/screenshots/P09.1-*.png`. Browse `http://localhost:<port>`.
- No existing test is loosened. The catalog still stores nothing personal.

## Out of scope
Deploying the catalog (owner-gated). Tags and releases (owner-gated). Runner, extension, or contracts code.

## Report

### 2026-09-22 — Revision 2 (iter-004, Opus escalation)

**PR:** https://github.com/radroid/workflow-catalog/pull/10 (`packet/P09.1` → `overnight/integration`). Code head `d6551846b7f77c9f6d32c43668c67d7412afd98f`: three commits on `bc516db` (`c01f8ef`, `2a7ee86`, `d655184`), then this report. It fixes what the second-pass reviews left: the reviewer's three issues and the UI critic's one issue plus small fixes. No new merge of `overnight/integration` was needed: integration only moved to `f06688b` (docs and logs), and the PR shows no conflict.

**1. Medium: an error result was cached for the hour (`lib/release.ts`).** The function registered with `unstable_cache` now throws a `ReleaseLookupError` on an `error` result instead of returning it. The exported `getCachedPackageRelease` wrapper turns the throw back into `{kind: "error"}`. The bundled `unstable_cache.md` doesn't say what a throw does, so I read Next 16.3.5's own `next/dist/server/web/spec-extension/unstable-cache.js`. On a cache miss the throw propagates before `cacheNewResult` runs, so nothing is stored. On a stale entry, the background refresh's `.catch` logs and returns `cachedResponse`, so a published `found` survives a failed refresh. `not_found` is still returned, so a 404 stays cached. The cap, abort and timeout code is unchanged. Tests (`tests/release-cache.test.ts`, +2): the registered function rejects on a 500 with "Checksum asset responded with 500." while `getCachedPackageRelease` resolves `{kind: "error", message: "Checksum asset responded with 500."}`; a 404 resolves `{kind: "not_found"}`. Vitest 5 defaults `clearMocks: true`, so each new test re-imports the module (`vi.resetModules()`) to read the function it registers. Red before the fix: `AssertionError: promise resolved "{ kind: 'error', …(1) }" instead of rejecting`. **Mutation proof:** returning the error instead of throwing fails with that same message. Making the wrapper rethrow instead of converting fails with `promise rejected "ReleaseLookupError: Checksum asset respon…" instead of resolving`. Both were restored byte-identical (`cmp`), and the file went back to 4/4 green.

**2. Must fix: the screenshots broke the fixtures policy.** All eight `docs/screenshots/P09.1-{install,template}-{light,dark}-{1280,390}.png` are re-shot full page, signed in through a real invite as **Ada Quill** (owner sign-in at `/admin`, Create invite, accept). A Playwright 1.63 script (kept under `/tmp`, not committed) drove `pnpm --filter catalog dev --port 3105`, with throwaway `OWNER_SECRET`/`SESSION_SECRET` and `CATALOG_PGLITE_DIR` under `/tmp`, browsing `http://localhost:3105`. `colorScheme` is set on the browser context, so the theme script sees it on load. Before each capture the script asserted:
- the header's `.who` is "Ada Quill" (8/8);
- `data-theme` matches the theme;
- no horizontal overflow (0 on all eight);
- axe-core 4.13.0, default rules: 0 violations (8/8);
- on /install, selecting each of the 8 command blocks copies exactly its commands and nothing else.

I then opened all eight images: "Ada Quill" is in the header of each. Revision 1's report line (U7, below) named a display name outside the policy; it is corrected in place, and that name no longer appears anywhere in the tracked tree (`git grep`). The server is stopped: `lsof -nP -iTCP:3105 -sTCP:LISTEN` is empty and no `next dev` process remains.

**3. Low: the doctor note.** It now reads: "Doctor prints its seven checks — the five below, plus the privacy switches and the eve pin — each marked [ok], [warn] or [FAIL], and ends with “All required checks pass.” when none is [FAIL]. On a first install, “Provider connected” stays [warn] (“the model has not been verified yet”) until one npm run doctor -- --live, which makes one short model call, or “Check the model” on the runner's status page." Sources: `formatDoctorReport`, `providerItem`, `runDoctor`'s `ok`, `runner/cli/doctor.ts` and `runner/ui/status.html`. A new drift test checks:
- every `[mark]` in the note is one of the marks in `formatDoctorReport`'s own `mark` record, read from `runner/lib/doctor.ts`, and all three appear;
- "All required checks pass.", "Provider connected" and "the model has not been verified yet" are in both the note and `doctor.ts`;
- every `npm run doctor -- <flag>` in the note is listed on the `doctor` row of `runner/README.md`'s Scripts table, and `doctor.ts` itself names `npm run doctor -- --live`;
- "Check the model" is in the note and in `runner/README.md`'s Doctor section.

**Mutation proof:** adding "(or npm run doctor -- --verify)" fails with `` expected '| `doctor` | The install checklist. `…' to contain '`--verify`' ``. Changing one `[FAIL]` to `[fail]` fails with `expected [ '[ok]', '[warn]', '[FAIL]' ] to include '[fail]'`. A first draft used plain `toContain` for the marks and let that second mutation through, because the note repeats `[FAIL]`; it was tightened before the commit.

**4. Low: the build-extension note** is now exactly "Run it anywhere inside the clone; outside it nothing builds." Checked without building anything: from `runner/`, `pnpm --filter @workflow-catalog/extension exec pwd` prints `…/extension` (exit 0). From a folder under `/tmp`, pnpm 11.17.0 prints `No projects found in "/private/tmp/p091-r2/outside"` and exits 0.

**5. Small fixes (UI critic).**
- **Codex.** The block is now `npm install -g @openai/codex` and `codex login`, and the Homebrew route is in the sentence ("With Homebrew, brew install codex works in place of the npm line."). New test: no command is, or carries, a shell comment (`/(^|\s)#/`). Putting the `# or: brew install codex` line back fails it: `expected '# or: brew install codex' not to match /(^|\s)#/`.
- **`apps/catalog/README.md`.** It now says to browse `http://localhost:<port>`: the `dev` script binds `localhost` only, which on macOS is `[::1]`, so `127.0.0.1` refuses the connection. Measured this run: `lsof` showed `TCP [::1]:3105 (LISTEN)`; `curl http://127.0.0.1:3105/` gave `Failed to connect … Couldn't connect to server`; `curl http://localhost:3105/` gave 200. I also added one sentence to "Template page and package releases": a failed lookup is never cached.
- **Pairing.** The caption "In a second terminal, in workflow-catalog/runner:" now sits above `npm run pair`. The note ends "Enter it on the extension's options page: right-click the extension's icon, then Options." (`extension/manifest.json` has `options_ui`, and `extension/README.md` names right-clicking the icon.)
- **Optional, done: Corepack caption.** The Corepack block is captioned "Node 25 or newer only:". The sentence above it stays verbatim, because the drift test matches it against `runner/README.md`. This adds an optional `commandsLabel` to `InstallPrereq` and `InstallStep`, rendered as `<p class="command-label">` outside the `<pre>`, so a copy never includes it.
- **Optional, done: `<wbr>` in the clone URL.** `urlBreakParts` (`lib/install-commands.ts`) splits a command after each "/" in an `https://` URL's path, and `CommandBlock` renders the parts with a `<wbr />` between them. At 390 the clone command now renders on two lines, "git clone https://github.com/" then "radroid/workflow-catalog.git", with no mid-word break. Before, it took three lines and split "workfl" / "ow-catalog.git". The capture script measured each character's line and asserted that every line starts at the space or at a `<wbr>`. In `tests/install-command-block.test.ts`:
  - the structural pin of `CommandBlock` now matches the new shape and is just as strict: nothing but the mapped commands, and only a `<wbr />` between a command's parts;
  - the prompt-concatenation guards now cover `part` as well as `command`;
  - new tests prove every command's parts join back to exactly the command, and pin the clone URL's three parts.

  **Mutation proof:** appending a character in `urlBreakParts` fails the join test (`expected 'npm install -g corepack ' to be 'npm install -g corepack'`). Dropping the "/" from the split's lookbehind, so it splits mid-word, fails the clone-URL test (`expected [ 'git clone https://', 'g', …(36) ] to deeply equal [ …(3) ]`). Each mutation was restored byte-identical.

**Verify chain** (this branch at `d655184`): `pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures && pnpm --filter catalog build` exited 0.
- `pnpm typecheck`: 6/6 projects.
- `pnpm test`: `packages/contracts` 235/235, `packages/job-assistant` 151/151, `extension` 130 passed + 2 pre-existing skips, `runner` 155/155 + eval 4/4 with 20 gates, and `apps/catalog` **168/168 across 26 files** (was 162: +2 release-cache, +3 install-command-block, +1 drift). `scripts/*.test.mjs`: 2/2.
- `pnpm -r lint`: 6/6.
- `pnpm check:fixtures`: clean.
- `pnpm --filter catalog build`: all 8 routes.
- `git status --porcelain`: empty afterwards.

**Skipped, and why:** nothing from the list. I saw that at 1280 the pair note's "right-click" breaks at its hyphen; that is ordinary hyphenation, so I left it. Nothing outside the allowlist was touched; `runner/` and `extension/` were read only.

**Assumptions:**
- The `<wbr>` goes after every path "/" of a URL, not only after `radroid/` as suggested. With only that one, the 390 layout is three lines ("git clone", "https://github.com/radroid/", "workflow-catalog.git"). With both it is two, and it still never breaks mid-word.
- For a failure that isn't a `ReleaseLookupError` (for example, from Next's cache itself), the wrapper's message is "Release lookup failed.", as in the reviewer's sketch. The page shows its "temporarily unavailable" state either way.

**One thing to sharpen next time:** name the screenshot display name in the packet's Acceptance ("signed in as Ada Quill") and have the capture assert it. `pnpm check:fixtures` scans text for emails and URLs, so a name inside a PNG passes every automated check.

---

### 2026-09-22 (Revision 1, iter-004 implementer, Sonnet)

**PR:** https://github.com/radroid/workflow-catalog/pull/10 (`packet/P09.1` → `overnight/integration`), head `e361906b2c3767111eeda8e8132fb20dc61538fd`. One revision round, addressing the reviewer's REVISE (3 issues, R1-R3) and the UI critic's REVISE (7 issues, U1-U7) on the first round's `989341c`.

**Merged `overnight/integration` in first (`3aea365`):** this branch predated P02.1 (`d9454f2`), which landed `runner/README.md`'s Corepack prose line the coordinator asked "Before you start" to match verbatim. Without it, the new drift-test wording check would have had nothing real to verify against locally. Confirmed zero conflicts via `git merge-tree` before merging; nothing in this packet's allowlist is touched by integration's side.

**R1 — the checksum cap didn't stop the download in production (`lib/release.ts`):** Next 16.3.5's patched `fetch` tees a cacheable response — one copy feeds the Data Cache and is read to completion internally regardless of what this file's own `reader.cancel()`/timeout did to its *own* copy (measured ~835 KB / ~5 s before returning), and a stale entry's background refetch bypassed this file's guards entirely (no signal, no cap, no timeout). Fixed by never letting the raw `fetch()` touch the Data Cache (`cache: "no-store"`, confirmed no `next: {...}` option anywhere) and caching the *parsed* result instead, via `unstable_cache` (`getCachedPackageRelease`, same `RELEASE_REVALIDATE_SECONDS` window; confirmed still fully supported in this repo's own bundled `node_modules/next/dist/docs/.../unstable_cache.md`). `readCappedText` now aborts the fetch's own `AbortController` in addition to cancelling the reader when the cap trips. Three tests: the existing cap-trip test now also asserts the captured fetch signal is aborted; a new test asserts `cache: "no-store"` and no `next` option; a new `tests/release-cache.test.ts` mocks `next/cache`'s `unstable_cache` (identity passthrough — the real one needs Next's request-scoped `incrementalCache` to invoke its wrapped function outside a server) to prove `getCachedPackageRelease` registers `keyParts: ["release"]` and `{ revalidate: RELEASE_REVALIDATE_SECONDS }`, and that a real call delegates through with `version` as a genuine argument. **All three mutation-proofed:** reverting the `abort()` call, swapping `cache: "no-store"` for `next: { revalidate }`, mutating `keyParts` to `["release-wrong-key"]`, and hardcoding the delegated call to `fetchPackageRelease("0.0.0")` each failed the right assertion; each reverted and reconfirmed green.

**R2 — the drift test didn't check order or labels (`tests/install-guide-drift.test.ts`, rewritten):** the prior version compared a hand-picked command slice; a reviewer mutation reordering steps, typo'ing a script, and rewording a checklist label stayed green through all of it. Now: `INSTALL_STEPS.map(s => s.id)` is asserted against a literal expected array; the README-block comparison is built from `inRunnerReadmeInstall` in `INSTALL_STEPS`' own order (not hardcoded ids); every `npm run <script>` referenced is checked against `runner/package.json`'s real scripts (exact-set and per-property); `runner/lib/doctor.ts`'s own source is read and its seven checklist labels extracted (`extractDoctorLabels`, handling both the six plain-string labels and the one template-literal label) and the first five compared against `INSTALL_CHECKLIST_ITEMS`; the load-extension step's wording is checked against `extension/README.md`'s Load-unpacked section; sections are now sliced to the next Markdown heading generically (`sectionToNextHeading`) instead of a hardcoded end-heading string. **Mutation-proofed** (all within this packet's own allowlist, never touching `runner/lib/doctor.ts` itself): renamed a step id, reordered two commands within a step, and typo'd a label in `apps/catalog/lib/install-status.ts` — each failed the corresponding new assertion with a clear diff; each reverted and reconfirmed byte-identical to HEAD.

**R3 / U5 — the doctor note was wrong:** doctor prints seven checks (the five shared with the catalog's checklist, plus `privacy` and the `eve` pin), not five, and per the new step order it now runs last. The `doctor` step's note says so, and the drift test's last two assertions verify both facts against `runner/lib/doctor.ts`'s real source and `INSTALL_STEPS.at(-1)`.

**U1 — Sources/Connections weren't real lists:** replaced the `.kv` grid of `<div>/<span>` (screen readers read it as one run of text, WCAG 1.3.1; a wrapped long label at 390px looked like a separate item) with `<ul className="plain-list"><li>`, reusing `.changelog-notes`' own recipe verbatim ("like the changelog list," as asked), in normal text colour since these are primary content. Confirmed live via Playwright's accessibility snapshot: both sections now report as `list`/`listitem` roles, not generic text. `.kv` itself is gone (`tests/globals.test.ts` asserts its absence, not just `.plain-list`'s presence). Reused for `/install`'s new "Before you start" prerequisites too.

**U2 — commands looked like separate commands once wrapped:** `CommandBlock` (`app/(gated)/install/page.tsx`) now renders one `<code className="command-line">` per command instead of one `\n`-joined blob, with a CSS hanging indent (`padding-left` + negative `text-indent`) so a wrapped continuation sits under its own command rather than flush left like a new one — no `"$ "` prompt or other decoration is ever added. New `tests/install-command-block.test.ts` proves this structurally (this repo's established convention for a Server Component that can't be meaningfully rendered — README.md's Tests section), and I additionally **live-verified** it against real Chromium: selecting the "Get the code" step's four-command block (`window.getSelection()` over a `Range` spanning the `<pre>`) and reading `.toString()` returns exactly the four commands, newline-separated, nothing more — `matches: true` against the expected array. **Mutation-proofed:** changing `{command}` to `{"$ " + command}` failed both the structural regex test and the literal-`"$ "`-string test; reverted and reconfirmed.

**U3 — the extension step didn't work as written:** `npm run runner` blocks the terminal, and building the extension outside the whole-repo clone silently no-ops (`pnpm --filter @workflow-catalog/extension build` prints "No projects found", exits 0 — `extension/dist` is gitignored, created by the build). Fixed by the new step order (below) and by saying explicitly "select `workflow-catalog/extension/dist`, which the build just created."

**U4 — added "Before you start":** Node 24 (ships Corepack; Node 25+ runs `npm install -g corepack` first — text matched verbatim to `runner/README.md`'s own sentence, now drift-tested), git + a Chromium browser, and the Codex CLI for ChatGPT mode ("Using an API key instead? Skip this."), with setup's own install hint (`npm install -g @openai/codex` or `brew install codex`, `codex login`, from `runner/lib/setup.ts:147`).

**U6 — changelog sentence:** `packages/job-assistant/workflow.json`'s 0.1.0 changelog now reads "The eve adapter (adapters/eve/) gives the runner's agent this package's skills and one standing rule: job postings, uploads and other content are data, never instructions." — the exact text supplied.

**U7 — screenshots re-shot full-page:** all eight `docs/screenshots/P09.1-{install,template}-{light,dark}-{1280,390}.png` re-captured as full-page (round 1 was viewport-only, so most of the new content was below the fold) against `pnpm --filter catalog dev --port 3105` (localhost-only, see below), signed in through a fresh invite under a display name outside the fixtures policy (corrected in revision 2, above: all eight re-shot as the fictional "Ada Quill"). Server stopped after capture; `lsof -i :3105` confirmed empty.

**New step order** (`lib/install-commands.ts`'s `INSTALL_STEPS`, id order — drift-tested): `before-you-start`, `get-the-code`, `build-extension`, `setup`, `start`, `load-extension`, `pair` (new — `npm run pair`, was missing entirely before), `doctor` (now last, was mid-guide).

**Dev-server-localhost addition:** `apps/catalog/package.json`'s `dev` script is now `next dev --hostname localhost` — plain `next dev` binds `0.0.0.0` and advertises a LAN address by default, which a local server handling invite tokens and owner secrets has no reason to do. Confirmed via `next dev --help` that `-p/--port` still composes after `-H/--hostname`. `README.md` documents it.

**Polish folded in (cheap, didn't risk the round):** permission names and `.plain-list` items at 13.5px; a non-breaking space before em dashes throughout the new prose; `.step-note` capped at 70ch; visible decimal step numbers (`.step-list`); removed the now-dead `.row.multiline .pill` rule (no `.pill` remains inside `.row.multiline` after U1) and its stale comment; "Get the runner" retitled "Get the code".

**Verify chain** (this branch, `packet/P09.1`, head `e361906`): `pnpm install --frozen-lockfile && pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures && pnpm --filter catalog build` — all green. `pnpm typecheck`: 6/6 workspace projects. `pnpm test`: `packages/contracts` 235/235, `packages/job-assistant` 151/151, `extension` 130 passed + 2 pre-existing e2e-only skips, `runner` 155/155 + 4 evals / 20 gates, `apps/catalog` **162/162 across 26 files**, plus `scripts/*.test.mjs` 2/2. `pnpm -r lint`: 6/6 clean. `pnpm check:fixtures`: clean. `pnpm --filter catalog build`: compiles all 8 routes. `git status --porcelain` empty after the full chain, including after the screenshot re-shoot.

**Skipped, and why:** none of R1-R3/U1-U7 were skipped. `runner/lib/doctor.ts`, `runner/README.md`, `extension/README.md` and `runner/package.json` were read-only throughout (outside the allowlist); every mutation proof that needed to disturb a "source of truth" fact instead mutated the catalog-side fixture that mirrors it (`install-commands.ts`, `install-status.ts`), which is the more faithful proof of what this drift test actually exists to catch.

---

### 2026-09-22 (iter-004 implementer, Sonnet)

**PR:** https://github.com/radroid/workflow-catalog/pull/10 (`packet/P09.1` → `overnight/integration`), head `989341ce1153a5e109278b069a47a2fe87ec013d`. CI green (`ci` pass in 1m31s; CodeRabbit skipped — automatic reviews disabled for this repo, matching every prior PR here).

**Install guide resynced (`lib/install-commands.ts`):** the "get-runner" step now carries the whole-repo clone `runner/README.md`'s "Install" section documents — `git clone https://github.com/radroid/workflow-catalog.git`, `cd workflow-catalog`, `corepack enable`, `pnpm install --frozen-lockfile`, `cd runner` — with the trailing `degit`/`npm install` path removed entirely. `setup`, `doctor`, and `start` stay their own steps (unchanged ids, matching `npm run doctor`'s three-way split), and `setup`'s note now explains the Codex CLI sign-in (`codex login`) ChatGPT mode needs, from `runner/README.md`'s Provider section. The `extension` step builds with `pnpm --filter @workflow-catalog/extension build` (from `extension/README.md`'s "Build") and loads `extension/dist` unpacked — the second `degit` fetch for `extension/` is gone, since the whole-repo clone already provides it.

**Drift test (`tests/install-guide-drift.test.ts`, new):** reads the fenced ```sh block between `## Install` and `## Scripts` in `runner/README.md`, strips inline `#` comments, and compares it against `INSTALL_STEPS`'s `get-runner` + `setup` + `start` commands concatenated in order (`doctor` is deliberately excluded — it's a real step mirroring the Scripts table's `doctor` row, not a line inside that particular fenced block). A second test does the same for `extension/README.md`'s Build block against the `extension` step. A third pins `INSTALL_CHECKLIST_ITEMS`'s five ids (`node`, `runner`, `provider`, `workspace`, `extension`) both as a literal array and against `runner/README.md`'s "### Doctor" section text. **Verified failing then restored:** changed `"corepack enable"` to `"corepack enablex"` in `install-commands.ts`, reran — the first test failed with a clear expected/received diff naming exactly that line; reverted, reran — 4/4 green again.

**Refusal nonce, action-level (`tests/refusal-nonce.test.ts`, new):** `next/navigation`'s `redirect()` throws a plain `Error` (not tied to any Next request context) whose `.digest` is `"NEXT_REDIRECT;<type>;<url>;<statusCode>;"` — confirmed empirically by calling it directly under plain Node — so `ownerSignInAction` and `acceptInviteAction` are callable directly with no mocking. Two tests each call an action twice with an identical refusal-triggering input (`ownerSignInAction` with the wrong `OWNER_SECRET`; `acceptInviteAction` with no `token`, both refusal paths that need no database) and assert the two redirect URLs' `n` query params both match `crypto.randomUUID()`'s shape and differ from each other. **Verified failing then restored:** stripped `&n=${crypto.randomUUID()}` from both `adminError` in `lib/actions/owner.ts` and `refused` in `lib/actions/invite.ts`, reran — both new tests failed (`/admin?error=wrong_secret` with no `&n=` at all); reverted, reran — 2/2 green again.

**Checksum cap (`lib/release.ts`):** `readCappedText`'s cap-trip branch now does `await reader.cancel(capError).catch(() => {})` before throwing, instead of only relying on the `finally` block's `reader.releaseLock()` — releasing a lock drops this function's own reference to the stream without telling the underlying connection to stop. New test in `tests/release.test.ts` drives a custom `ReadableStream` whose `cancel()` records that it was called; **verified failing then restored** (temporarily reverted the `reader.cancel()` call, reran — `cancelCalled` stayed `false`, test failed; restored, reran — green). The two existing stalled-body timeout tests now also assert `result.message === "Request timed out."`, not just `result.kind === "error"`.

**UI notes (`app/globals.css`, `app/(gated)/templates/job-assistant/page.tsx`):**
- Download card spacing: added `.card .lede.tight { margin-bottom: 0; }` — three classes outrank `.card .lede`'s two-class `margin-bottom: 14px`, which was winning the cascade over `.tight`'s single-class `margin: 0` regardless of source order (CSS specificity, not source order, decides a tie between rules of different selector class-counts). Scoped so the plain-`.lede` intro paragraphs elsewhere in a card (Sources/Connections/Browser permissions) keep their intentional 14px.
- Permission names: `.perm-list dt`'s `font-weight` dropped from `600` (tied with `h3`'s own `600`) to `500` — lighter than the card heading, still bolder than `dd`'s default `400`.
- Removed the "Required"/"Supported" pills from Sources and Connections: both list a fixed, always-true set (a required source is required by definition), so the pill repeated the same word on every single row and carried no information — confirmed by checking `workflow.json` and `template-labels.ts` for any field that could vary it, and finding none. `.kv` drops from a two-column `[1fr auto]` grid to one column (`1fr`) now that there's no value cell to pair with the key.
- 4 new regression tests in `tests/globals.test.ts`. **Verified failing then restored:** reverted both files to their committed pre-P09.1 state via `git show HEAD:...`, reran `globals.test.ts` — exactly the 4 new tests failed (16 pre-existing tests stayed green), confirming no incidental coupling; restored, reran — 20/20 green.

**Changelog note (`packages/job-assistant/workflow.json`):** replaced "Provider-neutral; the eve adapter (adapters/eve/) is a stub — P02 fills it in." with "The eve adapter (adapters/eve/) mounts the package's skills and its content-is-data instruction fragment into the runner's agent, declaring no tools of its own since job actions run as runner tools and bridge events instead." — grounded in `packages/job-assistant/adapters/eve/README.md`'s "What it contributes" section (read, not touched — outside the allowlist). Confirmed no test in `packages/job-assistant/test/` or `apps/catalog/tests/workflow-manifest.test.ts` pins the old or new text.

**Screenshots:** `docs/screenshots/P09.1-{install,template}-{light,dark}-{1280,390}.png` (8 files), captured via Playwright against `pnpm --filter catalog dev --port 3105` with `CATALOG_PGLITE_DIR` under `/tmp` and throwaway `OWNER_SECRET`/`SESSION_SECRET` shell values (never written to a file), browsing `localhost:3105` per `apps/catalog/README.md`. Signed in through a real invite as the fictional "Ada Quill" (matches the existing fixture convention). Dark screenshots confirmed `document.documentElement.getAttribute('data-theme') === "dark"` before capture (the theme script only reads `prefers-color-scheme` on load, so `emulateMedia` must precede a navigation, not just precede the screenshot). 390px confirmed `scrollWidth === clientWidth` (no horizontal overflow) on the template page, where the pill removal and `.kv` column change land. Server stopped before this report; `lsof -ti tcp:3105 -sTCP:LISTEN` empty.

**Verify chain** (this branch, `packet/P09.1`): `pnpm install --frozen-lockfile && pnpm typecheck && pnpm test && pnpm -r lint && pnpm check:fixtures && pnpm --filter catalog build` — all green. `pnpm typecheck`: 6/6 workspace projects. `pnpm test`: `packages/contracts` 235/235 (16 files), `packages/job-assistant` 151/151 (6 files), `extension` 130 passed + 2 skipped (132, 16 files — the 2 skips are pre-existing e2e-only cases, untouched), `runner` 153/153 (14 files) + 4 evals / 20 gates, `apps/catalog` **150/150 across 24 files** (was 139/139 at `dc36fc0`; +11 net: 4 `install-guide-drift`, 2 `refusal-nonce`, 1 `release` cancel-on-cap, 4 `globals.test.ts` UI-note regressions), plus `scripts/*.test.mjs` fixtures-policy self-test 2/2. `pnpm -r lint`: 6/6 clean. `pnpm check:fixtures`: clean. `pnpm --filter catalog build`: compiles all 8 routes. `git status --porcelain` empty after the full chain.

**Skipped, and why:** the packet's Out of scope list (deploying, tags/releases, runner/extension/contracts code) — untouched, matches the allowlist. Did not touch `runner/README.md` or `extension/README.md` themselves (explicitly read-only per the task rules) even though the drift test's existence means a future edit to either file's Install/Build section will need a matching `INSTALL_STEPS` update — that's the point of the test, not a gap.

**Assumptions:** the drift test's scope (which `INSTALL_STEPS` entries the runner/README.md fenced block is checked against) treats `doctor` as legitimately outside that specific block — it's a real, separate step already present before this packet, matching the Scripts table's `doctor` row rather than a line inside the Install section's own clone-through-start snippet. The packet's own prose listed `npm run doctor` as step 6 of 7 "steps", which I read as the full recommended sequence a person follows (paraphrased, not a literal transcription — it also elides "cd workflow-catalog"), not as a claim that `runner/README.md`'s fenced block itself contains that line; `runner/README.md`'s actual Install block (re-verified via `grep -c`, present exactly once) does not.

**One thing to sharpen next time:** the Playwright MCP server's screenshot tool only accepts absolute paths nested under the *main checkout's* directory tree (confirmed: it refused a `/tmp` path outright), which at first glance conflicts with this task's "never into the main checkout" rule — resolved by noticing a worktree at `<repo>/.claude/worktrees/<id>` is itself nested under that same tree, so an absolute path into *this* worktree's own `docs/screenshots/` satisfies both the tool's allowlist and the task's rule simultaneously. Worth stating that distinction explicitly in the task rules next time, since "the main checkout" and "any path under the main checkout's directory" are easy to conflate under time pressure.

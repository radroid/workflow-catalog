# Autonomous build loop: per-iter claims are local; state.json IS committed
/.loop/claims/

# Dependencies and builds
node_modules/
.pnpm-store/
.next/
out/
next-env.d.ts
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# Env and secrets (fixtures are fictional; real credentials never enter the repo)
.env
.env.*
*.pem
*.key
secrets/
keys/

# Hosting
.vercel/

# Local verification artifacts, OS and editor cruft
.claude/worktrees/
.playwright-mcp/
*.log
.DS_Store

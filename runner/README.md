# @workflow-catalog/runner

Placeholder, deliberately minimal. Packet **P02** scaffolds the real eve
project here with `npx eve@0.63.0 init .` (pin exactly; never `@latest`) and
decides dev/build mode per the spike in `docs/spec/research/eve-runtime.md`.
See `docs/spec/implementation/P02-runner-spike-and-skeleton.md`.

# P03.2 (#17): round-1 review of head 1ed5a91 (iter 007)

The Sonnet implementer opened #17. The code head is 2d44d15; the later commits touch only the report. CI run 35989603016 on 1ed5a91 is green, including the extension e2e.

## UI critic (Opus): REVISE, 5 issues

Scratch: `/tmp/wc-ui12-p032-scratch/`. `log.json` holds every action's focus trail, live-region text, the region's accessibility text and the line geometry. The folder also has `shots/` (174) and `sheets/` (13). The harness drives the real eve Client with a scripted stream.

**What holds:**
- The focused refusals say why.
- The 413 on paste is a sentence.
- Profile's messages put the consequence first.
- Tones are right in both themes, and amber appears only on decisions.
- One persistent live region: 35 actions, each announced once, including repeated identical messages.
- Focus is never on `<body>`, and a focused node is never rebuilt.
- axe finds nothing serious or critical in 102 captures.
- Contrast is 16.9–19.3:1 in the line and 6.8–7.2:1 for the tag.
- No sideways scroll at 390.
- No claims are saved after a cancelled or failed turn, even with a verified `extract_claims` result in the stream.

**Issues:**
1. **The pinned line takes three rows at 390, and at 640 for the longest message.**
   - Every message of 57 characters or more shows "LAST ACTION:" alone on the first row, then two rows of text: 67 px, where it was 48 px before.
   - The cause: `.text` is an atomic `-webkit-inline-box` with `max-width: 100%`, so it can't share a row with the tag.
   - Fix: P04's `.last-action-body` pattern. One inner wrapper holds `.tag` and `.text` inline, clamped to two lines with no padding, and the outer box keeps the padding. A probe with that pattern gives 2 rows and 48 px, with no sliver, for all 15 messages.
   - Sheets: `sheets/issue1-line-390.png`, `c390l-lines.png`, `d390l-lines.png`.
2. **A 413 on upload is announced as only "Refused: Not uploaded."**
   - Focus stays on the file input, and the reason sits only in the field error, which screen readers don't re-read (round-4 polish 1, still open for refusals from the server).
   - Fix: in `refused()`, pass a focused reason while the field still has focus. That also covers a server-refused Enter in a statement input.
   - Shot: `shots/b390l-upload-413.png`.
3. **A failed extraction shows codes and statuses.** Examples:
   - "The extraction failed: MODEL_CALL_FAILED: Model provider API request failed (HTTP 400)." Before this PR, the code came last.
   - "…provider limit (AI Gateway rate-limited the request.)."
   - "…fetch failed."
   Fix: `extractionRefusal` phrases each case for a person, with no code or status, and the detail stays in `ctx.log.warn`.
4. **On load, the region reads "LAST ACTIONNothing yet." at 640 and below.** Both HTML files still ship the tag without a separator.
5. **The committed screenshots.**
   - 8 of the 16 are 500 CSS px wide: every `*-dark-1280` and `*-light-390`.
   - Two "light" shots are dark.
   - All 8 Profile shots were taken scrolled, with the sticky line painted mid-page.
   - The report's "one line even at 390" and "two lines at 390" are wrong.
   - The paste-too-large shots show a text box edited for the screenshot.

**Polish:**
- The longest real message is 122 characters: "1 edit saved, 1 edit proposed. Accepted. Approval withdrawn: “…” needs your answer." Put the edits note after the action's own consequence.
- Three messages run 86–90 characters; they fit once issue 1 is fixed.
- "LAST ACTION:" now shows a colon above 640 px, which the walkthrough and the Jobs page don't. A visually hidden ": " would fix that and issue 4 together.
- "512 KiB" is jargon; "512 KB" is plainer.
- **Outside this round:** a Confirm that opens a question replaces the server's message with "An answer is needed.", which drops any "N edits saved." prefix.

## Reviewer (Opus): REVISE, 7 issues

Scratch: `/tmp/wc-rev-p032-r1-scratch/` holds the chain, merge and CI logs, `probes/` and `mut/`. The clones are `/tmp/wc-rev-p032-r1-{merge,base,p05}`.

**What holds:**
- **The chain at the head** is green, and `pnpm test` gave identical counts three times: runner 865, and evals 6/6 with 107 gates (onboarding 67/67).
- **CI 35989603016:** Playwright 37/37, nothing skipped.
- **Merges:** onto integration, clean and green. With #16 (P05) merged on top, clean, runner 986, evals 7/7 with 161 gates, and nothing failing.
- **The grep:** nothing classifies a turn outside `run-harness.ts`.
- **Real-`Client` tests:** both callers drive the real eve `Client`.
- **The split is behaviour-preserving,** and `runner/test/run-harness*.test.ts` is unchanged.
- **Deliverable 5 probes pass:**
  - a double call saves each claim once;
  - a result for another category is never saved;
  - results that fail the schema, or are errors, are skipped;
  - a real result followed by `turn.failed` saves nothing.
- **Budget:** neither the model check nor the extraction pauses it. Setup and doctor use `lib/live-check.ts`, not `checkModel`.
- **The eval workspace:** no other eval's tools write the career profile, including P05's.
- **Scope:** clean, and `profile-reducer.ts` changes only polish 2's message.

**Issues:**
1. **Behaviour changed beyond deliverable 5, and existing expectations were edited to match.** The report says those tests "pass unchanged apart from its import".
   - A timeout was 504 and a thrown error 502; both now answer `200 {ok:false}`.
   - `status` now holds the `TurnResult` status, so a turn that saved nothing answers `{ok:false, status:"ok"}`.
   - The wording changed.
   - Edited expectations: `onboarding-routes.test.ts:291,408,425,435,440-455,462,468,510,519,522`, `onboarding-extract-timeout.test.ts:55,238,254,275-279,288,304,317,329-330`, and `eve-gateway.test.ts`, which was rewritten outright. Its failed-turn message check became a pass-through.
   - No consumer breaks.
2. **A turn whose stream throws is no longer cancelled.** `classifyTurn`'s catch (`run-harness.ts:236-238`) doesn't cancel.
   - Probe: a session is created, then the stream is refused with a 401. At base that gives 502 and one cancel; at the head, 200 and no cancel.
   - `onboarding-routes.test.ts:468` flipped `cancelCount` from 1 to 0.
3. **The 90 s pin was deleted.** Setting `EXTRACTION_TIMEOUT_MS` to 9 s passes 865/865.
4. **The budget record contradicts the code, and the option has no test.**
   - The report says the model check pauses the budget, but nothing pauses it.
   - The comments at `run-harness.ts:113` and `:137-138` are wrong.
   - Ignoring the option passes every test.
5. **The route's own rules are untested or incomplete.**
   - Removing the category filter (`onboarding.ts:163`) passes.
   - A schema-valid result whose quote isn't in the source is saved.
6. **Polish 1 misses refusals from the server.** `refused()` (`onboarding.js:431-434`) passes no focused reason, so a 413 upload is announced only as "Not uploaded." This is the same as the critic's issue 2.
7. **The eval has no gate-by-gate mapping,** and the eval's header claims one exists. Scenario 3 lost "no new claim persisted for the fabricated quote" (7 → 6), with no named replacement.

**Nits:**
- **Report errors:**
  - Deliverable 4's record is false: `ui-pages.test.ts:67` loads happy-dom from the extension's install, and P03.1 is still open.
  - `checkModel` is called only from `routes/model.ts:36`, which has a ctx.
  - `routes/model.ts` is untouched.
  - `interpretModelCheck` remains, as the adapter.
  - No CI run id is recorded.
- **Tests that don't prove their claim:**
  - `onboarding-tools.test.ts`'s "writing nothing itself" doesn't assert that.
  - The `extract-claims-logic` test header names a destination for idempotency that doesn't exist; the real one is `profile-reducer.test.ts:180-191`.
- **Stale text:** `run-harness.ts:120`, `profile.js:269-275`, and README line 542 ("not yet called from a real route").
- **Unpinned details:**
  - Nit 1's byte measure isn't pinned: `.length` passes.
  - `boundedSourceBody`'s sentence applied to every route passes.
- **Timing:** a timed-out model check now waits up to about 10 s more, for `modelId()` and the cancel.

**Mutations that survive:** the category filter removed; the budget option ignored; the deadline at 9 s; the 413 sentence applied to every route; the byte measure changed to `.length`. The implementer's five, and the reviewer's five others, all fail tests.

## Decisions for revision 1 (Q1–Q12)

Revision 1 goes to the same Sonnet implementer.

- **Q1. The HTTP contract (reviewer 1): ratified, with fixes.**
  - Every non-ok extraction answers `200 {ok:false, status, message}`. No client reads the code, and `runTurn` never throws.
  - `status` never reads `"ok"` when `ok` is false: an ok turn with nothing to save answers `status: "no_result"`.
  - List every edited assertion in the report: file:line, old → new, and the Q or deliverable that justifies it.
  - `eve-gateway.test.ts` asserts the failed model check again: not ok, with its exact plain message.
- **Q2. Cancel when the stream throws (reviewer 2).** Granted in `classifyTurn`:
  - Cancel through the session in the non-abort catch path, and in the no-boundary branch, as P03's route did. Bound each cancel as the abort path does.
  - Restore `onboarding-routes.test.ts:468` to 1.
  - Add a real-`Client` test: the stream is refused with a 401, and one cancel is sent.
  - P08-A's and P04's run-harness tests must pass unedited. If one can't, stop and report.
- **Q3. Plain failure messages (critic 3).** `extractionRefusal` and the model check phrase each case for a person: the model's error, the provider's limit, eve or the network not answering, timed out, cancelled, waiting on an input, and no usable result.
  - No code, status or HTTP number; the consequence first; about 80 characters.
  - The detail stays in `ctx.log.warn`.
  - Tests assert each message exactly.
- **Q4.** Restore `expect(EXTRACTION_TIMEOUT_MS).toBe(90_000)`.
- **Q5. The budget (reviewer 4).**
  - Record the real decision: neither the model check nor the interactive extraction pauses the budget, with the reason. P08-B's carried item revisits it.
  - Fix the comments at `run-harness.ts:113`, `:120` and `:137-138`.
  - Tests: `false` doesn't pause; the default still pauses; a provider limit through `POST /api/model/check` or through the extraction route leaves the budget unpaused.
  - **Ratified:** the split into an exported `classifyTurn` and the `runTurn` wrapper. Correct the report's reason for it.
- **Q6. The route re-checks what it saves (reviewer 5).**
  - Before `s.extractClaims`, keep only claims whose quote is in the route's own source text, using the tool's own check. Count the rest as rejected.
  - Test the category filter and the quote check.
- **Q7. Server refusals carry the reason (reviewer 6, critic 2).** `refused()` gives the line a short reason while the field still has focus: the upload's 413, and a statement the server refuses. Add a DOM test for each.
- **Q8. The eval mapping (reviewer 7).**
  - Put the gate-by-gate mapping, old → new, in the report, and correct the eval's header.
  - Scenario 3 gets its lost check back as an eval gate: the tool's result leaves out the fabricated quote and counts a rejection.
- **Q9. The pinned line (critic 1 and 4, and the colon polish).**
  - Use P04's `.last-action-body` pattern (`runner/ui/jobs.html` and `jobs.css`): two rows and 48 px at 390 for every message.
  - The visible tag keeps the walkthrough's words with no colon. A visually hidden ": " separates the tag from the message, on load and after every action.
  - Granted: `runner/ui/onboarding.html` and `runner/ui/profile.html`, the `#last-action` markup only.
  - happy-dom tests: the region reads "Last action: Nothing yet." on load, and reads separately after an action.
- **Q10. Wording (critic polish).**
  - The edits note comes after the action's own consequence.
  - "512 KiB" becomes "512 KB".
  - A Confirm that opens a question keeps the server's edits note.
- **Q11. Screenshots (critic 5).** Retake all 16 after Q9:
  - true widths, with `window.innerWidth` confirmed;
  - the named theme;
  - Profile scrolled to the top;
  - no text box edited for the shot.
  Correct the report's line counts.
- **Q12. The report and the nits.**
  - Correct the record: deliverable 4, `checkModel`'s callers, `routes/model.ts`, `interpretModelCheck`, and the CI run id.
  - The "writing nothing itself" test asserts that the profile is unchanged.
  - The `extract-claims-logic` test header names `profile-reducer.test.ts:180-191` as the idempotency check's home.
  - Fix the stale comments at `profile.js:269-275`, and README line 542.
  - Pin the byte measure with a multibyte test.
  - Test that another route's 413 keeps its generic message.
  - Bound a timed-out model check's extra wait, or give the reason in the report.
- **Carried to P06:** the Jobs page's tag still runs into its message at 640 px and below (P04's copy of this pattern).

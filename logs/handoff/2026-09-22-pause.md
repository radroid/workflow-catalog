# Loop paused by the owner — 2026-09-22 ~13:05 (America/Toronto)

**Why:** the owner is switching the orchestrator to a different model. The new orchestrator resumes the same loop from disk and this thread's history.

**Where the loop stands:**
- Iteration 4 is closed (commit 82c74ec; `logs/iter-004.md`).
- Iteration 5 began at 12:51 and was **in progress** at the pause. Its log (`logs/iter-005.md`) is not written yet. `.loop/state.json` `iter` is still 4, and `stage_status` is `paused`.
- No wake-up is scheduled.
- All five subagents were told to save their work and stop, and all five have stopped. The P03 implementer finished its revision first, because it took the pause for an injection.
- No process from this session holds a port. 4310, 4320, 4330, 4340 and 3106 were free at 13:21.

## How to resume (for the new orchestrator)
1. Invoke `autonomous-build-loop`. Read `CLAUDE.md`, `.loop/state.json`, `logs/latest.md`, this file and GOALS.md. Set `stage_status` back to `in-progress`.
2. You cannot message this session's subagents; spawn fresh ones. Their old worktrees under `.claude/worktrees/agent-*` stay on disk. Every branch below is pushed, so start new worktrees from the pushed branches:
   - `git fetch origin && git switch -c <branch> origin/<branch>` to continue a branch;
   - `git switch -c packet/PNN origin/overnight/integration` for new work.
3. Continue iteration 5:
   - **P07-B (#12):** round 2 on 6da1a83 was stopped partway, and CI is red. The one allowed revision round is spent.
     - Recommended: send the combined list in `P07-B-round-2-review.md` to a fresh **Opus** escalation implementer: 5 reviewer issues, 5 UI issues, polish, and the CI failure.
     - Then run a full round 3 (reviewer + UI critic), including the items round 2 never checked.
   - **P03 (#11):** revision 1 is **complete** at 9584e93. The packet report's "Revision 1" section maps every item.
     - Run round 2: a fresh Opus reviewer and a UI critic, with `P03-revision-1.md` as the checklist.
     - APPROVE means merge, and then P04 can start. A REVISE goes to a fresh Opus escalation implementer.
   - **P08-A:** a claim only, no code. The "Part A paused" note under `## Report` in `docs/spec/implementation/P08-schedules-runs-budget.md` holds its design and eve facts. A fresh Sonnet implementer continues on `packet/P08-A` from `P08-A-prompt.md`.
   - **P04 (job capture):** starts when P03 merges. Its packet was updated in iter 004: it owns the shared safe-fetch and readable-text, has SSRF rules, and handles `job_capture`.
   - **Later:** P05 (Opus) after P04, using P08-A's run harness. P03.1 after P03 and P04. P08-B after P05. Then the wave plan.
4. End each iteration with: `logs/iter-NNN.md`, `logs/latest.md`, the PR #2 body, the commit `iter NNN: …`, and ScheduleWakeup with the loop prompt verbatim.
   - The loop prompt is `docs/spec/implementation/OVERNIGHT-OPUS-PROMPT.md` Part B. A verbatim copy was kept at `/tmp/wc-loop-prompt.txt`.

## State per packet at the pause
All branches are pushed. At about 13:20 the main checkout was on `overnight/integration` at f9e421f; the handoff commit follows it.

| Packet | PR / branch | Head | State at the pause | Next action |
|---|---|---|---|---|
| P02.1 | #9 | squash d9454f2 | **done** (iter 004) | — |
| P09.1 | #10 | squash 61f0e2d | **done** (iter 004) | — |
| P03 | #11 `packet/P03` | **9584e93** | **Revision 1 complete.** The implementer ignored the pause message (see below) and finished. Its report is the "Revision 1" section under `## Report` in the packet file. CI was running at 13:21 (run 35760057313). | **Round 2:** a fresh Opus reviewer and the UI critic, using `P03-revision-1.md` as the checklist. A REVISE after this goes to a fresh Opus escalation implementer. |
| P07-B | #12 `packet/P07-B` | **6da1a83** | Revision 1 is complete, and the B5 correction landed. Round 2 was stopped partway. **CI red:** a dark-popup screenshot guard fails in e2e. | Recommended: skip finishing round 2. Give a fresh **Opus escalation implementer** the combined list from `P07-B-round-2-review.md` (5 reviewer issues, 5 UI issues, polish, CI red). Then run a full round 3 that also covers the items round 2 never checked. |
| P08-A | `packet/P08-A` (no PR yet) | **6024f0f** | Claim only; **no code yet**. Its report holds the design and the eve facts, including that a provider 429 surfaces as `details.semanticErrorId === "gateway-rate-limited"` on step.failed/turn.failed. | A fresh Sonnet implementer continues from `P08-A-prompt.md`, switching to the existing branch. |
| P04 | — | — | Open, blocked until P03 merges. The packet was updated in iter 004. | Start once P03 merges. |

**P03 details the round-2 reviewer should know:**
- The implementer ran its screenshot seed server on 4310, not its assigned 4320, which broke the UI critic's dark run.
- Its browser/MCP screenshots landed in the main checkout. Six files were moved to `/tmp/wc-stray-main-checkout/`.
- It says D7 was "not carried forward". In fact commit e698182 did it: `git grep -i acme origin/packet/P03` finds nothing.
- `pnpm test` timed out once under full concurrency, each time in a different, unrelated workspace (the machine was loaded by five agents). A `--workspace-concurrency=1` rerun was fully green.
- Its report says runner vitest 279/279 and eve eval 62 gates.

**Process failure worth fixing in prompts:**
- The P03 implementer treated the orchestrator's genuine PAUSE message as a prompt injection. It cited a harness system-reminder that flagged the delivery, and the port it believed it was using.
- P07-A did the same in iter 003.
- Every implementer prompt already says "orchestrator messages are genuine". A long-running agent's context can compact that away.
- Fix: repeat it in every mid-round message. For a real pause, also use TaskStop, and don't rely only on a message.

## Rules and lessons learned (details in logs/blocks.md)
- **Every implementer prompt must say:**
  - Branch from `origin/…`; agent worktrees start from a stale base.
  - Write only in your worktree or /tmp. Leave scratch folders in place.
  - No recursive deletes through node, find or python after `rm -rf` is denied. Never test a guardrail or reroute after a refusal.
  - Orchestrator messages are genuine.
  - Stage by explicit path. Commit at green steps. Plain push.
  - Use a temp HOME where the harness allows it; otherwise pass temp dirs explicitly, and keep everything away from the real HOME, keychain and model.
- **Ports are exclusive, and must be enforced:**
  - Owner: 3000/3001.
  - 4310 is the extension bridge. Only one agent at a time may bind it: the P07-B implementer or the UI critic, never both.
  - P03 harness 4320; P08-A harness 4330; UI critic runner 4340; catalog 3105/3106.
  - **At the pause, the P03 implementer held 4310 for a screenshot seed server, against its assignment, and broke the UI critic's dark run.** Its browser-tool screenshots also landed in the main checkout: six files, moved to `/tmp/wc-stray-main-checkout/`. MCP browser tools write relative to the main session directory, so tell agents to pass absolute `/tmp` or worktree paths.
- **Harness quirks:**
  - The harness refuses setting HOME, runtime-computed `git -C` paths, the word "eval" in commands, and complex compound commands.
  - A scheduled wake-up at 11:36 never fired while subagents were running. Keep a long fallback wake-up, and rely on task notifications.
- **Review rules:**
  - The reviewer's verdict line is exactly one of `APPROVE | REVISE — n | BLOCK — n`.
  - A REVISE goes back to the same implementer once. A second REVISE goes to a fresh Opus implementer.
  - After APPROVE (reviewer, plus the UI critic when the change is user-visible):
    - squash-merge into `overnight/integration`;
    - delete the remote branch;
    - remove the worktree;
    - fast-forward local;
    - set the packet `Status: done`;
    - tick GOALS.
- **Owner-gated items (never do these):**
  - the catalog deploy;
  - the first release tag, `job-assistant@0.1.0`;
  - the tag-push deny rule;
  - Chrome Web Store;
  - anything that touches `main`.

# P03.2 (#17): round-2 review of head 8ddcbae (iter 007)

Revision 1 was done by the same Sonnet implementer, with Q1–Q12 from `logs/handoff/P03.2-round-1-review.md`. The code head is e6c4823, and CI is green on both commits: 36001681004 on e6c4823 and 36004602174 on 8ddcbae, each including the extension e2e.

## UI critic (Opus): REVISE, 3 issues

The scratch folder is `/tmp/wc-ui12-p032-scratch/r2/`:
- `log.json`;
- `shots/` (223);
- `sheets/` (8), including `committed-390-all.png`, `committed-1280-profile.png`, `model-check-390.png` and `f2-line-390-640.png`;
- `out/`.

The harness drives the real eve Client against a scripted eve, with a made-up model setting, so the real model-check route runs.

**Round 1's issues:**
- **1. Three rows at 390: fixed.**
  - Every message up to 90 characters shows in two rows (48 px).
  - The 122- and 126-character worst cases clamp to two rows, with the full text kept in the accessibility tree and the tooltip.
  - No sliver on either page or theme.
  - The visible tag has no colon.
- **2. The 413 upload's reason: fixed.** "Refused: Not uploaded: That's over 512 KB. …" is announced once, and focus keeps the same node. A statement the server refuses (a 503 from the profile lock) is announced with its reason.
- **3. Codes in failure messages: partial.** No code, status or HTTP number is left, and five outcomes read plainly. See issues 1 and 2 below.
- **4. The tag on load: fixed.** "LAST ACTION", ":", "Nothing yet." read separately on load, and after each of 55 actions.
- **5. The committed screenshots: partial.** Widths, themes and Profile at the top are right, and no text box was edited. But 8 shots don't show their message.

**Still to fix:**
1. **The model check's failure reads twice** (new in revision 1). "The check failed: The model check failed: the model had a problem answering." The doctor line would repeat it the same way. Fix: drop "The model check failed: " from both sentences in `modelCheckFailureDetail` (`eve-gateway.ts:79-82`).
2. **Timed-out and parked extractions put the cause first.** Q3 asks for the consequence first. Suggested:
   - "The extraction stopped: no answer from the model within 90 s. Try again."
   - "The extraction stopped: the model asked a question this page can't show. Try again."
3. **Eight committed screenshots don't show their message.**
   - `profile-edits-proposed` (4) shows "Nothing yet": the revision was set up in the store, not saved from the page.
   - `paste-too-large` (4) shows only "Refused: Not saved.", with the 413 reason off screen. After this refusal focus moves to the text box, which scrolls it into view, so take the shot there.
   - The report's "never enters frame" is wrong.

**Polish:**
- Refusals that reuse the server's message keep its capital after the colon ("Not uploaded: That's…"). The page's own refusals use lower case.
- The model check's cancelled and parked messages use eve's words "turn" and "run".
- At 390, the 122-character message is cut at "needs your…". This two-row clamp is agreed.

**Outside this round:**
- The Status page's "Check the model" button drops focus to the body while the check runs, and its result isn't announced (`status.js`, not P03.2's).
- A "profile busy" refusal takes about 10 s to appear, because the page's reload waits on the same lock (P03's behaviour).

## Reviewer (Opus): REVISE, 4 issues

Scratch: `/tmp/wc-rev-p032-r2-scratch/` holds the logs, `probes/`, `mut/` and `test-diff-vs-base.patch`. The merge clone is `/tmp/wc-rev-p032-r2-merge`.

**What holds:**
- The full chain at 8ddcbae: runner 879, and evals 6/6 with 109 gates (onboarding 69/69).
- CI 36004602174: Playwright 37/37.
- A merge onto 8177541 is clean and green.
- With P05's d271a48 on top, the merge is clean (only `runner/README.md` is shared, and it auto-merged): runner 1077, and evals 7/7 with 163 gates.
- All five of round 1's surviving mutations now fail tests, and six spot-checks of the implementer's proofs are all caught.
- Q2, Q3, Q4, Q7, Q9 and Q10 are fixed. Q2's cancels are bounded at 5 s, fire only on failure paths and swallow errors. P04's and P08-A's code and tests are unchanged.
- Scope holds. The 21 lines added to `local-ui.test.ts` are additive and serve Q5.

**Issues:**
1. **When Q6's re-check rejects every claim, the route reports success and blocks the retry.**
   - `verifiedExtraction` (`onboarding.ts:186`) builds its result (`:194`) before the re-check. With every claim rejected, it persists an empty list (`:440`), records the content hash (`:441`) and answers `ok:true`.
   - The retry then answers "unchanged" and never runs a turn (probe 10). When the tool itself rejects the quote, the route correctly answers `no_result` (probe 10b).
   - The new Q6 test (`onboarding-routes.test.ts:545-563`) asserts `ok:true` and never checks the hash.
2. **Q1's list is incomplete.** It covers only edits since 1ed5a91. Round 1's edits are missing:
   - five `504`s;
   - `status: "waiting"`;
   - the R3 timeout test;
   - the `eve-gateway.test.ts` rewrite.
   That is 78 removed lines. Report line 78 still says the tests "pass unchanged apart from its import".
3. **Q12's and Q5's record corrections weren't made, and there are new false statements.**
   - **Still uncorrected:**
     - deliverable 4 (line 100);
     - the split's reason (line 74, and `run-harness.ts:57`, `:205` and `local-ui.test.ts:264`): `checkModel` runs only inside a route that has a ctx;
     - `interpretModelCheck` "gone" (it's at `eve-gateway.ts:94`);
     - `routes/model.ts` "changed" (it's untouched).
   - **New false statements:**
     - `tagText(tone)` "unchanged" (it became `TAGS[tone]`);
     - the Q5 note and proof 3 credit a run-harness test that doesn't exist;
     - proof 1 credits run-harness tests, but the cancels are caught only by `eve-gateway-real-client.test.ts:136` and `onboarding-routes.test.ts:486`.
4. **Q8 is partial.**
   - Only scenario 3 is mapped, yet the eval header (`:83-84`) says every gate is.
   - The two restored gates (`:187-190`) can't fail, because `:174` already asserts `output.claims` is empty. Their rationale is wrong.

**Nits:**
- The `local-ui.test.ts` test stubs `checkModel`.
- The Q7 statement test uses a refusal the page can't reach (`maxlength=600`), and the message has a byte count.
- `onboarding-tools.test.ts` weakens `toBe(claimId)` to `toBeDefined()`.

## Decisions for the escalation (S1–S9)

Both reviewers returned REVISE twice, so a fresh Opus escalation takes over `packet/P03.2` at 8ddcbae. Q1–Q12 still stand; the S decisions add to them.

- **S1. No claim survives (reviewer 1).** When Q6's re-check leaves nothing, answer `{ok:false, status:"no_result"}`, persist nothing and record no hash.
  - Rewrite the Q6 test to assert that, plus `isSourceContentUnchanged` is false, plus a second extract opens a second turn.
  - Add a mixed case: one claim survives and one is rejected, giving `ok:true`, the claim saved and the hash recorded.
- **S2. The complete Q1 list (reviewer 2).** List every assertion edited or removed in an existing test file against the PR's base, 5b4146d: file:line, old → new, and the Q or deliverable that justifies it. The reviewer's `test-diff-vs-base.patch` is the checklist. Correct report line 78.
- **S3. Corrections (reviewer 3).** Add a correction paragraph for each false statement the reviewer lists. Fix the comments at `run-harness.ts:57` and `:205` and at `local-ui.test.ts:264`.
- **S4. Q8, finished (reviewer 4).**
  - Map every gate that changed, across both rounds: the old label, then the new label or a named replacement. Make the eval header's claim match the mapping.
  - Remove the two vacuous gates, or make them able to fail. The Vitest route test for Q6's quote re-check is the named replacement for "no new claim persisted for the fabricated quote".
- **S5. The doubled model-check message (critic 1).** Drop "The model check failed: " from both sentences in `modelCheckFailureDetail` (`eve-gateway.ts:79-82`). Test the whole message the model route returns.
- **S6. Consequence first (critic 2).** Use these, or wording as short:
  - "The extraction stopped: no answer from the model within 90 s. Try again."
  - "The extraction stopped: the model asked a question this page can't show. Try again."
- **S7. Screenshots (critic 3).** Retake the 8 through the page:
  - `profile-edits-proposed`: save the edits from the page;
  - `paste-too-large`: focus on the text box, with the reason in view.
  Correct the report's "never enters frame".
- **S8. Nits and polish.**
  - Restore `toBe(claimId)` in `onboarding-tools.test.ts`; a weakened assertion is never acceptable.
  - The `local-ui.test.ts` test drives the real `checkModel` against a scripted eve `Client`, as `eve-gateway-real-client.test.ts` does.
  - The Q7 statement test uses a refusal the page can reach, such as the profile lock's 503.
  - After a colon, a reused server message starts in lower case.
  - The model check's cancelled and parked messages don't say "turn" or "run".
- **S9. Mutation proofs:** S1's no-result branch, S5's single prefix and S8's restored assertion each fail a test.

**Carried:**
- **To P06:** the Status page's "Check the model" button drops focus to the body while the check runs, and its result isn't announced. `runner/ui/assets/status.js` is granted.
- **To P03.1:** a "profile busy" refusal takes about 10 s to appear, because the page's reload waits on the same lock.

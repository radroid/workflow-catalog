# P06.1 (#18): round-1 review of head e41aefd (iter 007)

The Sonnet implementer was pulled forward alongside P05's revision 3. CI 36093077520 on e41aefd is green, including the extension e2e (Playwright 37/37). No existing test was edited: all 4 test files are new.

## Reviewer (Opus): REVISE, 5 issues

Scratch: `/tmp/wc-rev-p061-r1-probes/`, `/tmp/wc-rev-p061-r1-logs/`. The merge clone is `/tmp/wc-rev-p061-r1-merge`.

**What holds:**
- **Items:** 1.3, 1.4, 1.5, 1.7, 2.2, 3.1, 3.2 and 4.2.
- **Contrast,** computed from the tokens:
  - `.secondary` border: 7.55–8.46:1 (was 1.27:1);
  - `.error` text: 6.29–7.59:1 (was 3.84:1).
- **The rest:**
  - 18 mutations rerun: all caught, except the CSS ones (visual, as allowed) and issues 1 and 2.
  - The chain is green at the head and merged onto integration: runner 908, evals 107 gates.
  - Scope is clean. P05's branch shares only `runner/README.md` with this PR, with no conflict.

**Issues:**
1. **4.1:** the test doesn't prove the busy guard. Deleting `status.js:112` passes all 4 Status tests. Count `POST /api/model/check` and expect exactly 1.
2. **1.6:** the test doesn't prove the notice clears. A no-op `clearUnreachable` (`jobs.js:930`) passes all 8 page tests. Add "up while still running", then a second outage.
3. **1.2:** the combined message names no job when every name is under 16 characters: "2 jobs: 2 were extracted." (`jobs.js:910`). P05's `settledMessage` has the same loop.
4. **1.1:** only focus inside `#detail-structured` is handled. With focus on the "Full posting text" toggle or the address link, the stale section stays for good.
5. **2.1:** the cause is no longer logged, and the refusal isn't plain.
   - The route gives `500 extraction_not_queued`, and `bridge.logs` gains nothing.
   - The page falls back to "the runner hit a problem", and nothing names the file.
   - Refuse as T6 does, with a 409 naming `jobs/<id>/extraction-<rev>.json`.

**Nits:**
- 1.3 over-cuts when the cut lands at a word's end.
- A cut can leave a dangling "·".
- The counted fallback names no job.
- 1.4's announcement uses a second name.
- 1.7 reads ": " for one frame.
- 2.2:
  - "0 revisions";
  - a uuid-named regular file under `jobs/` lists as an unreadable folder, with a 200 detail where the base gave 404.
- 4.1 has no visible busy style.
- 4.2's advice names the runner on a page the runner served.
- The four Status 390 shots are 531 px wide.
- The report has several inaccuracies.
- The model check's error path shows literal backticks.

## UI critic (Opus, ui14): REVISE, 7 issues

Scratch: `/tmp/wc-ui14-p061-scratch/`: `stage-{a..i}.mjs`, `out-stage-*.txt`, `log.json`, `shots/`, `sheets/`, `shorten-check.mjs`. Applications was checked through a scratch merge with P05 at 00ff33e.

**What holds:**
- **Items:** 1.2, 1.3, 1.5, 1.7, the rows and headings of 1.4, the list and detail of 2.2, 4.1 (focus, screen readers, one request per press) and 4.2.
- **The shared styles** hold on every runner page in both themes.
- **Jobs and Status** meet the rules, apart from issue 4.

**Issues:**
1. **1.6: a first-load notice never clears.** The first-load error branch (`jobs.js:640-646`) never sets `unreachableShown`, and `clearUnreachable` (`:934`) doesn't accept the first-load text. A first-load 500 stays too. P05's `application.js` handles both.
2. **1.1:** the same as reviewer 4, shown with the Address link, the summary and its text.
3. **Screenshots:**
   - These four are 531 px wide, from the long temporary workspace path: `P03.2-status-model-check-failed-{light,dark}-390.png` and `P06.1-status-model-check-success-{light,dark}-390.png`.
   - Twelve Jobs shots were captured scrolled, so the sticky line sits mid-page: `name-consistency`, `focus-after-unreadable` and `unreadable-directory`.
4. **Status: the check announces "Failed to fetch"** when the runner stops mid-check (`status.js:125`). Jobs and Runs already say "Can't reach the runner. Is it still running?".
5. **Status: no visible busy state.** `runner.css` has no `[aria-disabled="true"]` rule, and before this PR native `disabled` dimmed the button.
6. **1.4: the "Last action" line uses a second name** (`jobs.js:1049`, and the settle path).
7. **2.2: "0 revisions" is false** (`jobs.js:607`).

**Polish:** `shorten` drops a word that fits; a dangling "·…"; the first "Checking…" isn't announced; the eve URL isn't in `<code>`.

**Outside this round:**
- On Status, "New pairing code" and "Revoke" use native `disabled`, so focus drops to the body, and their errors show raw text.
- The checklist "Fix:" lines show literal backticks.
- A 57-character custom workspace path overflows Status at 390.

## Decisions for revision 1 (K1–K12)

This goes to the same Sonnet implementer, as its one revision round. `runner.css` is also granted for K9's busy rule, and for K10's path wrap if you put it there. Each K item gets a test that fails without its fix, or a UI-critic check if it's purely visual. Each tested item also gets a mutation proof.

- **K1. The busy guard is proven (reviewer 1).** The test counts `POST /api/model/check` over three presses and expects exactly 1.
- **K2. The runner-down notice always clears (reviewer 2, critic 1).**
  - After a watched outage, the next good refresh clears the notice, even while the extraction is still running ("Extracting …" returns). A second outage is announced again.
  - A first-load failure (refused, or a 500) sets the same state, and clears on the next good refresh.
  - Test all three.
- **K3. Names in the combined message and the word cut (reviewer 3, polish).**
  - With short names, each job is named once: "Extracted “QA · Quill” and “Chef · Harbor”.".
  - The cut keeps a word that fits, and never leaves a dangling "·" or separator before the ellipsis.
  - The counted form names at least the failure.
- **K4. Focus anywhere in the detail (reviewer 4, critic 2).** When no revision is readable, focus anywhere in the detail (the address link, the "Full posting text" summary or its text) moves to the heading before the stale sections are dropped. Test each.
- **K5. A failed waiting-state write (reviewer 5).**
  - Log the cause with its path.
  - Refuse plainly, as T6 does: a 409 that names `jobs/<id>/extraction-<rev>.json`.
  - The page maps the code to a plain message that names the file.
- **K6. One name (critic 6).** The "Last action" line uses the same name as the row and the heading, for a job whose latest file is damaged.
- **K7. The unreadable folder's row (critic 7, reviewer nit).**
  - The row doesn't claim a revision count.
  - A uuid-named regular file under `jobs/` is skipped as before: it isn't listed, and its detail answers 404.
- **K8. Status page messages (critic 4, polish, reviewer nits).**
  - A check that can't reach the runner says "Can't reach the runner. Is it still running?", never "Failed to fetch".
  - The first "Checking…" is announced.
  - The eve URL is in `<code>`.
  - The eve line's advice names eve, and says how to start it again.
  - Server text shown on the page (the model check's error path and the checklist's "Fix:" lines) renders backticked commands as `<code>`, never literal backticks.
- **K9. A visible busy state (critic 5).** Add a `[aria-disabled="true"]` rule to `runner.css`, so a busy control looks busy. Check every runner page that uses `aria-disabled`, and report any that already had its own busy style.
- **K10. The Status page at 390 (critic 3's cause, outside the round).** A long workspace path wraps, with no sideways scroll at 390, even for a 57-character custom path.
- **K11. The pairing buttons (critic, outside the round).** "New pairing code" and "Revoke" use `aria-disabled` rather than native `disabled`, keep focus, and give plain error messages.
- **K12. Screenshots and the report.**
  - Retake the four Status 390 shots at a true 390 with a short workspace path.
  - Retake the twelve Jobs shots from the top of the page.
  - Add shots for K2 (a first-load outage, then clearing), K5 and K9 (busy against idle).
  - Correct the report: the 1.1 test writes invalid JSON rather than using chmod; the dangling "see 'critical bug' below"; contrast per page; the true screenshot widths.
  - List every edited existing assertion, with file:line, old → new, and the K that justifies it.
  - Then the full chain, and CI green including the e2e.

**Round 2** uses the same reviewer and critic. After it, a REVISE goes to a fresh Opus escalation.

**Carried:**
- **To P06,** on the Applications page: P05's `settledMessage` has the same short-name loop as reviewer 3. It names no application when every name is under 16 characters. The same page already carries the mid-word cut and the counted form's missing names.

# P02.2 (#15): round-1 review of head db8cbf9 (iter 006)

CI 35959588048 on db8cbf9 is green, with Playwright 37/37. Revision 1 goes back to the **same Sonnet implementer**, whose worktree is kept, with decisions W1–W8 below. A second REVISE goes to a fresh Opus escalation.

## Reviewer (Opus): REVISE — 3 issues

Scratch:
- `/tmp/wc-rev-p022-r1-pr.diff`;
- `/tmp/wc-rev-p022-r1-scratch/`: the probes and their outputs, including `probe-precedence.out`, plus the backups, the CI log and the chain logs;
- `/tmp/wc-rev-p022-r1-probes/`: the probe workspaces.

**What holds:**
- **Precedence.** With `.env.local` naming A, A wins over an ambient valid workspace B, over a non-workspace folder (the GitHub Actions shape), and over a relative path. An empty, blank or space-padded identical value gives A with no warning. With no file, no key, or a blank value, the environment's B is used. All other keys still let the environment win.
- **Commands.** The launcher (`cli/runner.ts:75`) hands eve A. `runner`, `pair`, `ui`, `doctor` and `--forget` all go through `loadSettings()`.
- **Doctor's warning** is plain, names both paths, and is a warning, not a failure.
- **Leaks: none.** Only doctor reads `workspaceEnvOverride`. The extension's `GET /status` is unchanged.
- **Tests were only added to:** 104 lines added, and no assertion removed.
- **Chain.** The chain at the head and on the merge onto 9b28d3f is green. CI ran every step.

**Issues:**
1. **The `setup.ts` change wasn't needed. It lets the ambient value choose and save the first-run workspace** (`runner/lib/setup.ts:205-212`, the fallback at 210).
   - Before the PR:
     - a re-run already resolved A;
     - a first-run `--yes` without `--workspace` failed with "Pass --workspace <folder>";
     - the interactive default was `~/JobAssistant`.
   - Now:
     - a first-run `--yes` adopts the ambient B and writes it, creating the folder if it's missing;
     - with the GitHub Actions value, setup fails, naming a folder nobody chose;
     - the interactive default becomes the ambient value.
   - The user-facing text is now wrong: `cli/setup.ts:14` and `:96`, `README.md:109` and `mvp-spec.md:76`.
2. **Doctor warns falsely when the ambient value is the same folder, spelled differently** (`settings.ts:142` compares strings, and `doctor.ts:191-203` shows the warning).
   - A trailing slash, `/tmp` against `/private/tmp`, a symlink, `..` segments, and a difference in letter case all warn "… which is ignored".
   - Following the warning's own fix doesn't clear it.
3. **`--forget` targets the environment's workspace when `.env.local` has none** (`forget.ts:77`; with `--yes`, `cli/setup.ts:51,56,61`).
   - With no `.env.local`, or no workspace key in it, and a valid ambient B, the plan offers to delete B, which holds the person's profile, jobs, applications, sessions and runs.
   - This predates the PR, and the reviewer asked for a ruling. The ruling is W3.

**Nits:**
- **N1.** Deliverable 4 has no test. Swapping the spread order at `runner.ts:75` (M4) passes all 569 tests.
- **N2.** No test uses a B that exists as a valid workspace. `forget.test.ts:121-126`'s "never B" holds trivially.
- **N3.** Doctor's fix line (`doctor.ts:200`) says `npm run setup`, which keeps A. It should say `npm run setup -- --workspace <path>`.
- **N4.** `README.md:130-131` says "unlike every other key below", and "below" is wrong.
- **N5.** When A is missing, doctor's failure (`doctor.ts:206`) doesn't mention the ignored ambient value.

**Mutation proofs:**
- M0 (the implementer's): 4 tests fail.
- M1 (suppress the warning): 1 fails.
- M2 (warn becomes fail): 1 fails.
- M3 (the merge-base `setup.ts`): 1 fails, the first-run half only.
- M4 (the launcher's spread order): survives.

## Revision 1: the orchestrator's decisions (W1–W8)

You may read, and copy from, `/tmp/wc-rev-p022-r1-scratch/` and `/tmp/wc-rev-p022-r1-probes/`. Don't edit them.

- **W1. Setup never takes the workspace from the environment** (issue 1).
  - Revert `runner/lib/setup.ts`'s workspace lines to the merge base. Keeping `trimmedText` is fine.
  - A first-run `--yes` without `--workspace` still fails with "Pass --workspace <folder>". The interactive default stays `~/JobAssistant`. A re-run keeps A.
  - The environment is only a runtime fallback for commands, as in tests. It is never a choice setup makes for the person.
  - Rewrite the first half of the setup test to prove that a first run with a valid ambient B never adopts it. Keep the re-run half.
- **W2. Doctor compares folders, not strings** (issue 2).
  - Warn only when the environment names a different folder.
  - Compare `fs.realpathSync.native` of both, falling back to `path.resolve` when a path doesn't exist.
  - Add tests for a trailing slash, a symlink and `..` segments, each with no warning, and for a different folder, with a warning.
- **W3. Forget removes only the workspace recorded in `.env.local`** (issue 3, the orchestrator's ruling). Forget deletes what this install stored; deletion is irreversible, so it fails safe.
  - A workspace that comes only from the environment is never offered for removal.
  - Forget names it in a note: the environment points at `<path>`, which forget leaves alone because setup didn't record it.
  - Record the source on the settings, for example a `workspaceSource` field.
  - Test with a valid B on disk, three ways: with no `.env.local`, with an `.env.local` that has no workspace key, and with `--yes`.
- **W4. The launcher is tested** (N1).
  - Move the construction of eve's environment into a small exported helper in `runner/cli/runner.ts`, with no change in behaviour. This file is granted for that alone.
  - Test that the resolved workspace overrides an ambient one. M4 must then fail a test.
- **W5. The tests use a B that exists as a valid workspace** (N2), including the "never B" case for forget.
- **W6. Doctor's fix line** says `npm run setup -- --workspace <path>` (N3). When A is missing, the failure also mentions the ignored ambient value (N5).
- **W7. Wording.** Fix `README.md:130-131` (N4). With W1, `cli/setup.ts:14` and `:96`, `README.md:109` and `mvp-spec.md:76` become true again; check that each one is.
- **W8. Proofs.**
  - Re-run M0.
  - Each of these must fail a test:
    - setup adopting the ambient value (the old M3);
    - the string comparison (W2);
    - forget offering an environment-only workspace (W3);
    - M4 (W4).
  - Run the full chain, check that `git status --porcelain` is empty, and get CI green.

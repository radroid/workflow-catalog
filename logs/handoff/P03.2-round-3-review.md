# P03.2 (#17): round-3 review of head eb22ad8 (iter 007)

Revision 2 was done by a fresh Opus escalation, with S1–S9 from `logs/handoff/P03.2-round-2-review.md`. CI run 36014650001 on eb22ad8 is green, including the extension e2e.

## UI critic (Opus): APPROVE

Scratch: `/tmp/wc-ui12-p032-scratch/r3/` holds `log.json`, `shots/` (226), `out/` and `sheets/`. The harness drives the real eve Client against a scripted eve.

**Fixed:**
- **S5.** Every model-check failure reads once on the Status page, as "The check failed: …" plus a lower-case clause. Eight kinds were checked, including a real 90-second timeout.
- **S6.** "The extraction stopped: no answer from the model within 90 s. Try again." and "The extraction stopped: the model asked a question this page can't show. Try again." Each is announced once, focus stays put, and nothing is saved.
- **S7.** All 28 new or changed screenshots are at true widths and in the right themes, and each shows its named message.
- **S8's polish.** Refusals use lower case after the colon, and the model check never says "turn" or "run".
- **The regression sweep:**
  - the pinned line is two rows at 390 and 640;
  - the separator reads correctly on load;
  - one live region, and 49 actions each announced once;
  - focus never lands on the body;
  - axe finds nothing serious or critical in 102 captures;
  - contrast holds;
  - no sideways scroll.

**Judged:** the upload refusal's field error partly below the fold at 390 is acceptable. The pinned line always shows and announces the same reason, and scrolling would move the focused control.

**Carried:**
- **To P03.1:** P03's page-load error lines on both pages:
  - they keep the server's capital after the colon;
  - "Failed to fetch" is the browser's own wording, with no full stop;
  - "profile busy" gives two retry instructions in a row.
- **To P06, with the other Status-page items:**
  - In light theme, the model check's failure sentence is #e54b4f on white, 3.85:1 (axe serious). The colour comes from `.error { color: var(--destructive) }` in `runner.css`.
  - The eve line shows the eve client's raw error text.
  - The two 1280 `P03.2-status-model-check-failed` shots include a harness-only line, "eve is not answering…". Retake them once the Status page changes.
- **To P10 part B:** `npm run doctor --live` (`cli/doctor.ts:58`) prints "The model check failed: The model answered, …", with a capital after the colon.

## Reviewer (Opus): APPROVE

Scratch: `/tmp/wc-rev-p032-r3-scratch/`. The merge clone is `/tmp/wc-rev-p032-r3-merge`, with branches `r3-merge` and `r3-merge-p05`.

**Fixed, all four round-2 issues and S8:**
- **S1.** Probe 10 now gives `no_result`: nothing saved, no hash recorded, and the retry opens a second turn. Three more probes behave:
  - a mixed result saves the survivor and records the hash;
  - both checks rejecting gives `no_result`;
  - text changed mid-turn gives `no_result`, and the retry saves.
- **S2.** All 158 removed lines in the 11 changed test files are on the report's list, with reasons. That includes the 504s, `waiting`, the R3 test and the `eve-gateway.test.ts` rewrite.
- **S3.** All nine corrections are true, and no "before a RunnerContext exists" wording is left.
- **S4.** The eval diffed from base to head: 65 → 67 gates, with each change mapped. The two vacuous gates are gone, and the header names the replacement test exactly.
- **S8.**
  - `toBe(claimId)` is restored; with `toBeDefined()`, the same mutation passes.
  - The `local-ui` test drives the real `checkModel`.
  - The statement test uses the profile lock's reachable 503.
  - Refusals use lower case after the colon, and the model check never says "turn" or "run".

**Mutations:**
- S1, S5 and S8 each fail tests.
- Ignoring the budget option fails exactly two tests.
- Removing both Q2 cancels fails exactly two.
- Making the pause off by default fails 10.

**The new helpers:** `scripted-eve.ts` runs eve's real `Client` with only `fetch` replaced. The model-route and budget-option tests drive the real routes and files.

**Leftovers:**
- `run-harness.ts:12` is a stale comment and goes to P08-B.
- `doctor --live` prints a single prefix, since `live-check.ts` never used the gateway's wording. Its capital after the colon predates the PR and goes to P10-B.

**Chain, CI and merges:**
- **The chain at eb22ad8:** runner 891 tests; evals 6/6 with 107 gates.
- **CI 36014650001:** Playwright 37/37.
- **The integration merge:** eb22ad8 already contains integration at 9afdbd7, so the chain at the head covers it.
- **With P05's 6a4354d on top:** clean; runner 1092; evals 7/7 with 161 gates; nothing failing.
- **Scope:** clean.

**Minor, not counted:**
- `seconds()` is defined twice, identically, at `eve-gateway.ts:73` and `routes/onboarding.ts:80`.
- A line citation is off by a few lines.
